# Batch 311 — 待确认生成 确认/取消交互：自动化不可达确认（源站 2026-09-11）

> 状态：`BLOCKED_MANUAL`（重建流程脆弱 + 确认入口未现；无代码变更）。
>
> 证据：`post-submit.png`、`probe-log.json`（本批尝试记录）。

## 记录

- 待确认生成 状态重建失败：本轮 首帧芯片 点击未命中（视频节点位置
  漂移）、发送定位误中底部工具栏按钮——重建链路三步全脆弱；
- 待确认生成 占位态的 确认/取消 入口在占位节点上不可见（无按钮
  渲染于 batch 310 截图）——确认交互可能为占位符整体点击或需
  素材库/其它入口，自动化不可达。

## 补充视觉证据（batch 317 采样时捕获：`pending-node-with-slots.png`）

待确认生成 占位节点的**需求槽以音频/图片缩略图形式直接渲染在节点
底部**（音频 00:04 波形缩略 + 图片 1080×1446 缩略）——需求槽的
节点内嵌形态首次完整可视。

## 判定

**待确认生成 确认/取消交互 转入 BLOCKED_MANUAL**（同 batch 291
清单第四类的 需人工点击 类）。自动化侧已尽力：三批探针（277/278/
311）。

## 验收

- docs check 通过；源站画布 **0 残留**（reload-then-clean 规则）；
  无代码变更。
