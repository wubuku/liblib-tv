# Batch 210 — logo 下拉菜单重采 + 几何对齐

## 源站事实（窗口 rAF ~31fps；`source-logo-menu.json` / 截图存档）

- logo 下拉菜单 **200×190**，四行 191×44：回到主页 / 全部项目 /
  创建新项目 / 删除项目，分隔线位于创建新项目之前——与 clone 的
  Batch 106/119 四项合同**逐字一致**。
- 仅几何差：菜单宽 160（w-40）→ **200**；行高 36（h-9）→ **44**（h-11）。

## 实施

- `TopNavBar` ProjectMenu：`w-40` → `w-[200px]`，行 `h-9` → `h-11`；
  分隔线位置与四项顺序不变（全部项目路由合同保留）。

## 验收

- `verify-liblib-batch210.py`：6 checks（打开/200 宽/四项顺序/行高 44/
  创建前分隔线/0 page error）。
- 回归绿：119（logo 菜单 16 checks）/ 22 / 172。
- `npm run check`：0 errors（8 warnings 基线）；docs check 通过。
- 不证明：删除项目/创建新项目点击后的源站行为（clone 为本地占位，
  Batch 119 合同）；菜单内图标（源站四行均为纯文本）。
- 源站测试残留清理：0 残留（本批无源站节点操作）。
