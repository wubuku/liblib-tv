# Batch 264 — 资产抽屉漂移对齐实施（源站 2026-09-10 采样落地）

> 状态：`IMPLEMENTED`（batch 263 采样 → 实施 → 12 checks 验收 → 38 项
> 回归绿）。
>
> 源站证据：`../liblib-canvas-batch263-2026-09-10/drawer.png`。

## 对齐内容（batch 263 采样事实四项）

1. **宽度 280 → 320px**（batch 202 的 ~280 为旧采样）；
2. **列表视图切换图标**：工具行（`data-asset-manager-listview`，
   aria-pressed 视觉开关——菜单语义未采样，CLONE_DECISION）；
3. **条目悬停操作**：条目 `group` 化，悬停显示 `… 菜单` + `发送`
   图标（点击行为未采样，视觉呈现）；
4. **页脚重排**：折叠箭头图标（ArrowLeft，保留
   `data-asset-manager-collapse` 与 aria-label）移至左下 +
   `data-asset-manager-count` 计数（共 N 节点/项资产）——布局与源站
   截图一致，且避开了底部工具栏（left-64）的几何重叠。

## 实施过程记录

首版把箭头放页脚右端，与底部工具栏（`left-64`）几何重叠导致点击
互斥——对照 batch 263 截图（箭头在左下）重排后消除。

## 验收

- `verify-liblib-batch264.py`：**12 checks**（320 宽；列表视图存在/
  初始 off/可切换；条目悬停操作出现；计数 + 折叠箭头 aria/svg；点击
  折叠关闭抽屉）。
- 回归绿：21 / 22 / 26 / 33 / 100 / 111 / 125 / 128 / 146 / 149 / 151 /
  155 / 160 / 165 / 166 / 172 / 173 / 174 / 175 / 176 / 177 / 178 / 189 /
  191 / 213 / 215 / 218 / 236 / 237 / 238 / 239 / 240 / 244 / 248 / 249 /
  252 / 255（38 项）。
- `npm run check`：0 errors（8 warnings 基线）；docs check 通过。

## 不证明

- 列表视图切换的菜单/视图语义（未采样，仅视觉开关）；
- 条目悬停操作的点击行为（… 菜单内容、发送目标）；
- 评级过滤与列表视图的组合行为。
