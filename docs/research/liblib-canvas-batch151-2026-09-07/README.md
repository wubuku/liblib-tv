# Batch 151 — 采样第二轮 + 工具行/积分块微对齐（源站 2026-09-07）

## 源站事实（第二轮 DOM 取证）

### 1. 生成面板绑定选中态（对齐确认）

取消选中（点击空白画布）→ 生成面板关闭（参考工具行消失）；重新选中 → 面板重开。
clone 的 `showSingleNodeEditor = selected && selectedNodeCount <= 1` 行为一致，
无需修改。

### 2. 「尝试：」建议列为一次性入口

首次选中未选模式的视频节点展开卡 → 显示 3 个纵向 pill 建议；点击任一 → 面板
打开、建议列消失；此后再次选中 → 直接打开面板，不再出现建议列。clone 当前为
常驻尝试行（batch125/128 契约），差异记录，待受控复测后再定重构。

### 3. 分组内含两个视频节点

「预设 - 首尾帧生成视频」分组实际节点：首帧/尾帧 image 节点（469×350）+
**两个** video 节点 —— 350×350 普通输出节点与 622 宽承载面板的节点。
分组外下方另有首帧/尾帧两张源图（260,1033 / 260,1433）。

### 4. 菜单类浮层受 rAF 门控，节流窗口无法打开

模型/参数菜单（floating-ui/Mantine Popover）打开依赖 requestAnimationFrame 定位；
背景节流窗口中 rAF 冻结 → 真实鼠标点击/JS 全事件序列均不挂载菜单。
因此「菜单选中态样式」「300s 参数展开」「Auto 比例积分数据点」仍待窗口前置后采样。
（chip 点击、面板开关等纯状态更新不受影响 —— 与 Batch 149 勘误一致。）

### 5. 已对齐项（本 batch 实施）

- 工具行 pill：h=26、`rounded-full px-2 py-1`（原 h-7 px-2.5）。
- 积分块：`min-w-[85px]`、右对齐（justify-end）、fg-muted 灰调（原金色
  #d6a233）、数值 12px/15px。积分公式不变（135 数据点回归断言保持）。

## 验收

- `verify-liblib-batch151.py`：7 checks 通过（pill 高度/积分块几何与灰调/135 回归）。
- 相邻回归绿：21 / 22 / 125 / 149。
- `npm run check`：0 errors、8 warnings（既有基线）。
