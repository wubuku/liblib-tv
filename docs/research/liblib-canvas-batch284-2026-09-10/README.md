# Batch 284 — 工具箱「使用」行为：无可见画布变化（源站 2026-09-10）

> 状态：`SAMPLING_FINAL`（工具箱自动化探索穷尽；无代码变更）。
>
> 证据：`post-use-state.png`（使用后画布 + 工具箱全貌）。

## 源站事实（`SOURCE_FACT`）

- 工具箱预设项为**带真实缩略图的视频模板卡**（左弧滑行=西装男演示、
  电商手机弹出=耳机、咖啡杯出场=咖啡、360旋转=耳机粉底、机械臂视角、
  Live 2D=动漫角色——每项一个模板视频示例）；
- 面板形态：底部工具栏**上方浮层**（含 ? 帮助 + × 关闭），非左停靠；
- **「使用」点击后无可见变化**：画布节点数/边数/选中态不变，视频面板
  页脚不变——预设应用很可能在生成请求内部生效（模板跟踪生成），
  UI 层不可观察；结合该账号无法生成（batch 278），完整语义不可达。

## 工具箱实施判定

clone 实施价值有限：使用行为不可观察（生成依赖账号），仅可复刻
浮层 UI 与模板卡清单。**暂缓**，待源站行为可观察时再实施。

## 方法学备注

- 工具箱浮层位于底部工具栏正上方（非左停靠）；
- json import 缺失导致两次 heredoc 探针中途崩溃——**heredoc 探针
  脚本头部必须 import json**（反复踩坑，固化为规则）。

## 验收

- docs check 通过；源站画布 **0 残留**；无代码变更。
