# Batch 266 — 图片面板默认态确证（源站 2026-09-10 直采）

> 状态：`SAMPLING_RECORDED`（默认态三源一致确证；图片高清态仍
> `SOURCE_UNKNOWN`）。`.selected` 实时重定位法生效。

## 源站事实（`SOURCE_FACT`）

**图片节点默认态（未提交尝试）**：模型 **Lib Image**、设置芯片
**16:9 · 标准画质 · 2K · 1张**、积分 **18**——batch 263 / 265 / 266
三次独立读数一致，Lib Image 基准价 18/张 确立。

## 图片高清态（仍 `SOURCE_UNKNOWN`）

`.selected` 内部点击法下，点击「图片高清」后页脚读数全部丢失（面板
状态消失）。两种解释：

- 图片高清为**直接动作芯片**（点击即发起高清生成，无面板态）；
- 或点击引发了脱选。

倾向前者（ upscale 语义不改变面板参数），但无截图证据
（`gaoqing.png` 为点击后状态，可复核）。

## 方法学沉淀

`.selected` 实时重定位 + 内部安全偏移（0.25 宽 / 0.18 高）点击有效——
默认态读数由 batch 265 的全空变为完整捕获。

## 验收

- docs check 通过；源站画布 **0 残留**；无代码变更。
