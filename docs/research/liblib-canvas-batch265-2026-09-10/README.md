# Batch 265 — 图片节点生成面板采样（部分受控读数，源站 2026-09-10）

> 状态：`PARTIAL`（图生图态受控读数确证；默认态/图片高清态因选中
> 脆弱性未获干净读数）。

## 受控读数（`SOURCE_FACT`，img-tushengtu.png）

- **图生图尝试提交态**：模型 **Lib Image**、设置芯片
  **16:9 · 标准画质 · 2K · 1张**、积分 **18**——与 batch 263 的 18
  观察值互证（Lib Image 基准价 18/张）。

## 未确证（`SOURCE_UNKNOWN`）

- 默认态（未提交尝试）的芯片与积分（选中点击易脱选，面板开合脆弱）；
- 图片高清态读数（点击后面板状态漂移）；
- Lib Image 模型菜单全量目录（预计与 batch 234 的图片模型 7 项一致，
  未直接枚举）；
- 设置菜单展开态（画质/分辨率/张数维度全貌）；
- 积分公式（仅 1 个受控数据点）。

## 方法学备注

图片节点的面板常驻性弱于视频节点：点击尝试芯片后若命中节点边缘，
选中即丢失（面板关闭）。后续采样需先 store 级锁定选中（或用
`.selected` 类实时重定位点击）。

## 验收

- docs check 通过；源站画布 **0 残留**；无代码变更。
