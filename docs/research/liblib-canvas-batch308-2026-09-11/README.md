# Batch 308 — 工作流/退出跟随 深探：空画布下均薄结果（源站 2026-09-11）

> 状态：`THIN`（空画布下两项均无强特征；无代码变更）。
>
> 证据：`exit-follow.png`、`workflow-click.png`、`viewmode-log.json`。

## 记录

### 退出跟随

- 点击后按钮**迁移位置**（x≈970, y≈9——原顶栏位 → 另一工具位）且
  标签仍含「跟随」（可再次切换）；
- 视口 transform 变化（translate(106px, -1236px) scale(2)）——跟随
  退出伴随视口平移/缩放调整；
- 语义与 clone 的 batch 92 视口跟随合同域一致。

### 工作流

- 空画布下点击**无可见视图差异**（仍是 双击画布 自由生成节点 空态 +
  建议芯片）——工作流视图的差异需要画布内容（分镜组等）才能显现；
- 深探需带内容的画布（与 batch 278 账号限制无关联，纯采样前置）。

## 下一迭代要点

工作流视图差异深探的前置：先建 分镜组/多节点 内容再切换 工作流/
故事板 对照。

## 验收

- docs check 通过；源站画布 **0 残留**；无代码变更。
