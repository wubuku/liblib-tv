# Batch 276 — 2.5 提交同样被会员门拦截（源站 2026-09-10 直采）

> 状态：`SAMPLING_RECORDED`（免费模型提交流留待下一迭代；无代码变更）。
>
> 证据：`post-1.png`（付费墙全页，标题与 batch 275 相同）、
> `submit-log.json`。

## 源站事实（`SOURCE_FACT`）

模型切回 **Seedance 2.5**（页脚确认 2.5、积分 230 = Auto·720P·5s）后
填写提示词并提交：

- **同样弹出会员付费墙**，标题不变「正在使用会员专属模型，升级会员
  继续使用」——付费墙的「每月生成数量」表也列出 Seedance 2.5 720P
  档位（33秒→1,435秒），印证 2.5 为会员档内模型；
- 任务不创建，积分未扣（提交前 230 读数为面板预估值，非扣费）。

## 综合判定（batch 275 + 276）

**非会员账号下，主流视频模型（2.5/2.0 VIP 系）提交时全部被会员门
拦截**。非会员可用的提交路径预计仅存在于特定免费模型（1.x Lite /
Pixverse 系等——batch 141 目录中的非 premium 项），未采样。

## 下一迭代候选

切换到免费模型（如 Seedance 1.0 Lite）重试提交，采样非会员任务创建
与 pending 态——与 batch 33 的 clone pending 合同对照。

## 方法学备注

- 提交后积分读数 4,714 为付费墙配额表数字的误读（读数器无弹窗感知），
  非真实余额——真实余额 topbar ⚡100 未变；
- 付费墙以 Escape 关闭。

## 验收

- docs check 通过；源站画布 **0 残留**；无代码变更。
