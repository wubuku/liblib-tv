# Batch 302 — 抽屉筛选联动采样不确定（源站 2026-09-11）

> 状态：`INCONCLUSIVE`（第二个节点未建成，列表仅单类型——过滤联动
> 无法分辨；无代码变更）。
>
> 证据：`filter-linkage-log.json`、`filter-image.png`。

## 记录

- 建视频节点后，第二次 dblclick+文本 建节点未成功（面板/首节点遮挡），
  画布仅 1 节点 → 抽屉列表仅 视频节点 14 一条；
- 筛选菜单 开/选图片/选全部 三步列表均不变（单类型下无法分辨联动）；
- 筛选菜单本身再次确认可开（aria-label 驱动 + 坐标点选均命中）。

## 下一迭代要点

需先确保画布上有 ≥2 个**不同类型**节点再开抽屉——第二节点创建须在
面板关闭后（Escape）再 dblclick 空白处。

## 验收

- docs check 通过；源站画布 **0 残留**；无代码变更。
