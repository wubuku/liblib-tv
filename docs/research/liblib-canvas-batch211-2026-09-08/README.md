# Batch 211 — 角色库模态重采（部分成功，尺寸规则开放）

## 采样结果（窗口 rAF ~31fps；`source-clib-current.json` / 截图存档）

- 角色库模态成功打开并转储：**1280×710 @ (320,58)**，文本含
  「公共角色库 / Seedance2.0&2.5合规素材库」双页签——承诺书门在公共
  页签不出现（与 Batch 169 一致），素材库页签未再进入（维持不代同意）。
- 与 clone 对照：CharacterLibraryPanel 为 **1304×731**（Batch 111 于
  1440 视口、2026-09-05 采样）。两日两视口的读数无法推导尺寸规则
  （1280/1920=0.667 非流式比；1304/1440=0.906）——固定值差异可能是
  视口/采样条件差异，**记开放问题，不动 clone**。
- 二次开模态失败（页面状态漂移，JS click 不再触发），多视口采样留待
  后续窗口期。

## 处置

- 零代码改动（尺寸规则未证实，不盲改 Batch 111 合同）。
- 采样 JSON/截图存档于本目录。

## 验收

- 证据存档完整；`npm run check` 基线不变；源站画布 0 残留。
- 开放问题移交：模态尺寸规则多视口采样；素材库门后内容（仍待用户
  授权）。
