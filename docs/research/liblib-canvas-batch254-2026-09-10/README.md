# Batch 254 — 首尾帧态芯片消失/恢复补采 + 155 读数修正（源站 2026-09-10 直采）

> 状态：`SAMPLING_RECORDED`（补采批次，无代码变更）。
>
> 证据：`a-committed.png`、`c-reselect.png`、`sample-log.json`、
> `reselect-log.json`。全程源站文本/几何探针。

## 源站事实（`SOURCE_FACT`）

1. **芯片提交后消失确认**：首尾帧芯片提交后，节点上三枚尝试芯片
   全部不再渲染（源站文本探针空读）；
2. **去选 → 重选后芯片不恢复**：点击空画布取消选中，再经节点标题
   文本（视频节点 N）重选——芯片仍不存在，且页脚保持提交态
   （2.0 / 全能参考 / 16:9 · 720P · 5s · 1个 / 135）。提交态为
   **节点持久态**，非面板瞬态；
3. **135 = 27×5（平价率表精确吻合）**：batch 251 的 155 读数为
   切换过渡态的瞬时值，**「全能参考模式溢价」假设撤销**。模型平价率
   表（2.0→27/s）不受影响。

## Clone 发散记录（后续批次候选）

- clone 的 `attempt` 为 VideoNode **组件态**：去选重选后尝试建议会
  复现——与源站的「提交态持久化」发散。收敛需将 attempt 迁至节点数据
  （涉及 batch 177 重放联动与 batch 239/252 守卫的联动调整，
  单独立批实施）。

## 验收

- docs check 通过；源站画布 **0 残留**；无代码变更。
