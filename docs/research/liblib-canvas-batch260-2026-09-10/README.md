# Batch 260 — OmniHuman 音频满足路径探测：无上传供体确认（源站 2026-09-10）

> 状态：`PARTIAL`（Probe A 负结果确认；多把手拖拽探测 Probe B 留待后续）。

## 结果

- **Probe A（音频槽容器点击）**：「音频 0/1」标签向上 5 层祖先中不存在
  40–70px 的槽容器盒——需求行的 DOM **只有标签文本，没有任何上传
  供体/可点击容器**；
- 该负结果与 batch 257 的「标签点击无文件选择器」合并结论：**音频需求
  在 DOM 层面不存在上传路径，连线是唯一满足机制**（图优先范式加强）；
- Probe B（多 target 把手枚举 + 逐个拖拽探测）：留待后续批次（需完整
  状态重建 + 逐把手边差分，成本较高）。

## 验收

- docs check 通过；源站画布 **0 残留**；无代码变更。
