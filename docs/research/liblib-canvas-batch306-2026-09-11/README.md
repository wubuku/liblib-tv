# Batch 306 — 顶栏图标点击行为全量扫雷（源站 2026-09-11 直采）

> 状态：`SAMPLING_RECORDED`（八个顶栏按钮逐一点击；两新表面发现；
> 无代码变更）。
>
> 证据：`tb-*.png`（逐按钮截图）、`topbar-log.json`。

## 源站事实（`SOURCE_FACT`）

| 顶栏按钮 | 点击行为 |
|---|---|
| 画布 1 | 画布切换下拉（batch 286 已采） |
| 工作流 | 无可见面板（疑为视图模式切换，待深探） |
| **故事板** | **打开 Agent 抽屉**（400px 右栏，与 打开 Agent 同面板） |
| 退出跟随 | 无可见面板（视口跟随模式切换，batch 92 合同域） |
| **发布与分享** | **浮层**：在LibTV上发布 / 发布你的作品和创作过程 / 分享链接（拥有此链接的人可以查看并复制你的画布） |
| **积分超市** | **全页弹窗**：限时限量倒计时 + 灵感快冲（立省900元 30,000 豪华版及以上专享）——与 batch 275 付费墙同体系 |
| ⚡100 积分 | 同 积分超市 弹窗 |
| 打开 Agent | Agent 抽屉（batch 273/301 已采） |

## 发现

1. **发布与分享浮层**为新表面（分享链接语义——原型可低成本复刻）；
2. **故事板 图标 = Agent 抽屉第二入口**（非独立故事板视图）；
3. 积分超市 与 ⚡100 共用同一弹窗（batch 275 付费墙同体系）；
4. 工作流/退出跟随 无面板打开——视图模式类切换（行为待深探）。

## 实施排程建议

发布与分享浮层：低成本可复刻（浮层结构 + 分享链接行）。

## 方法学备注

探针末尾簿记崩溃（LOG 键重赋值）不影响数据——逐按钮截图已存档。

## 验收

- docs check 通过；顶栏面板已关闭还原；源站画布 0 残留；无代码变更。
