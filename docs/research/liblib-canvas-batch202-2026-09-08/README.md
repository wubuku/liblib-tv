# Batch 202 — 资产管理抽屉重采 + 宽度对齐

## 源站事实（窗口 rAF ~31fps；`source-asset-drawer.json` / 截图已存档）

- 左下「资产管理」打开**左侧栏**（实测内容右缘至 x≈270 → 抽屉宽约
  **280**；clone 原为 240）。
- 结构：顶部项目名上下文条；**画布 / 资产** 双页签；「所有评级」筛选；
  画布页签空态「画布暂无节点」；底部「共 N 节点」计数。
- clone 文案与结构自 Batch 102/114 起已逐字一致——本批仅宽度差。

## 实施

- `AssetManagerPanel` 容器 `w-60`（240）→ `w-[280px]`（源站直采）。
- 文案/页签/筛选零改动（已对齐）。

## 验收

- `verify-liblib-batch202.py`：10 checks（抽屉 280 宽/画布资产页签/
  所有评级/计数格式/资产页签列出现有媒体并切回/0 page error）。
- 回归绿：102（13 checks）/ 114（20 checks）/ 121 / 172。
- `npm run check`：0 errors（8 warnings 基线）；docs check 通过。
- 不证明：「所有评级」筛选菜单的展开选项（Batch 102 记为未采样，维持）；
  资产页签在有上传资产时的列表项结构（本次仅验证现有节点列出）。
- 源站测试残留清理：0 残留（本批无源站节点操作）。

## 补充（Batch 204，同日续采样）

- 页面刷新后资产抽屉呈**常驻左侧栏**形态（非覆盖层）：搜索图标、滑杆
  图标、所有评级 ˅、视图切换；「展示设置」文本按钮在此形态下不存在。
- 滑杆图标打开**节点类型筛选菜单**（180×369，10 项逐字：全部/文本/图片/
  视频/智能剪辑/导演台/逐帧拉片/音频/脚本/脚本（旧版）——与添加面板
  节点类型一致）。
- 实装：clone 的「展示设置」改为 toggle 该类型筛选菜单
  （`data-asset-manager-typemenu`，10 项）；batch102 的展示设置 hint
  断言迁移为菜单断言（14 checks）。
- batch202 验证器同步：资产页签断言改为列出现有媒体（预设画布非空，
  空态文案合同归 batch102）。
- 不证明：筛选后的列表联动（源站未采样）；两种抽屉形态（覆盖层 vs
  常驻侧栏）的切换规则。
