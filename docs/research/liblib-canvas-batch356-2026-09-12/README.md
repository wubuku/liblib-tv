# Batch 356 — batch 61 定案：三处确定性修复（`VERIFIER_FIXED`）

> 状态：batch 61 从 sweep 失败恢复**确定性三连绿**；台账 §5.z3
> 更新（61 → 已修复转绿）。无产品代码变更。源站恢复探测：
> `RECOVERY: still-broken`。

## 三处修复（逐层插桩定位）

1. **回调段 pane 取消选中**：三个大 TextNode + 胶囊行已覆盖整个
   929×874 视口（scan 实证：10 个候选点全部命中节点内容区/pill），
   硬编码 (780,730) 命中 TextNode 内容区并选中 node-c——setup 改用
   store 级 `selectElements` 取消选中（确定性）；被测的边透明路径
   点击仍走真实 UI；
2. **移动段边线在视口外**：390px 下边线中心 (515,325) 超出视口——
   ⌘0 适合屏幕后再点；
3. 插桩清理。

## AGED_GATE 清算进度（**9/13 已转绿**）

- 已转绿：29（338）、39（346）、40（347）、41（348）、46（349）、
  48（353）、49（354）、57（355）、**61（356）**、64（351）——
  其中 64 为专项；
- 维持归档：6（marquee 历史化，AGENTS.md 在档）、
  89（移动面板生命周期取代，batch 339 已有结论）。

## 验收

- batch 61 三连跑全绿；docs check 通过（915 Markdown）；
- `npm run check` 0 errors；无产品代码变更。

## 后续候选

- 源站恢复后 BLOCKED_SOURCE 补采与 CLONE_DECISION 替换；
- 89 复核（可选：以 339 结论 + 确定性关闭交互收尾）。
