# Batch 378b — 相机预设 append 语义入册（`DEC-048 RESEARCH_GATE`）

> 状态：batch 352 的开放问题正式登记为 **DEC-048**（RESEARCH_GATE）。
> 无产品代码变更。liblib.tv 恢复重测：`RECOVERY: still-broken` 维持。

## DEC-048 内容

- **决策**：相机运动预设 append 为纯拼接（`[...track.keyframes,
  ...generated.slice(1)]`，自 8125872 未变——batch 352 考古）；
  「清除窗口内冲突键」无源站证据支持；
- **状态**：RESEARCH_GATE——待源站恢复采样裁决（录一次 append 观察
  源站是否清除既有键）；
- **现状**：验证器已按纯拼接语义断言（batch 361 迁移，三连绿）；
- **解锁条件**：源站模型菜单/预设面板可交互。

## 关联

- batch 352（考古定案）、batch 361（验证器迁移）；
- 台账 §5.z3（44 行）已注明该产品待裁决。

## 验收

- docs check 通过（936 Markdown）；无 src 变更。
