# Batch 161 — 视频面板增高 397px（修复纵向开关列引入的溢出）

## 缺陷（测量发现）

Batch 149 将高级设置改为纵向列（约 156px）后，固定 274px 的面板内容超订阅：
- 提示词 textarea 被压缩到 **16px**（一行）；
- 高级设置区底部越界 section 边界 **35px**。
既有验证器从未测量提示词高度，回归未被捕获。

## 修复

- 面板 section `h-[274px]` → `h-[397px]`：提示词恢复 **95px**（源站实测 96px，
  2026-09-07）、高级设置回到边界内（余量 9px）。
- 验证器迁移：batch21（面板高度 397；参数菜单偏移 normal -244.5 / long -196.5）、
  batch22（模型菜单 -209.5）、batch26（面板高度 397）。

## 验收

- `verify-liblib-batch161.py`：4 checks（高度 397 / 提示词 88-102 / 无越界 / 0 diagnostics）。
- 相邻回归绿：21 / 22 / 26 / 33 / 125 / 128 / 155 / 160。
- `npm run check`：0 errors、8 warnings（既有基线）。
