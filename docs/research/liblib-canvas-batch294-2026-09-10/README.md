# Batch 294 — 画布表面覆盖盘点与后续批次规划（batch 236–293 总结）

> 状态：`PLANNING`（盘点文档；无代码变更、无源站操作——代码自
> batch 281 后未变，batch 288 的 41 项确认仍有效）。

## 一、已采样并对齐的表面（batch 236–293 实施项）

| 表面 | 批次 | 状态 |
|---|---|---|
| 模型平价率表（16 模型受控 + 14 数据点 + 1080P 表 + 默认清晰度 + 触发器标签） | 236–242 | ✓ 实装 |
| 首帧芯片流（自动图节点/参考槽/销毁） | 239/244 | ✓ 实装 |
| 首尾帧芯片流（双图节点/预填文案/模型 2.0） | 252 | ✓ 实装 |
| attempt 持久化（节点数据） | 255 | ✓ 实装 |
| 工具行 pill 集分布（Happy Horse/Wan 2.6 变体） | 248 | ✓ 实装 |
| OmniHuman 特殊面板（需求槽/自适应/快速模式） | 249 | ✓ 实装 |
| 资产抽屉 320px + 列表视图 + 悬停操作 + 页脚箭头 | 264 | ✓ 实装 |
| 生成历史 全部画布 scope chip | 281 | ✓ 实装 |
| Agent 抽屉第五 headline | 273 | ✓ 实装 |
| 键盘快捷键 ⌘ 系对照（⌘A 双边不全选） | 292/293 | ✓ 确认 |

## 二、已采样确认（无漂移/账号态边界）

| 表面 | 批次 | 结论 |
|---|---|---|
| 预设容器（标题可编辑、无折叠） | 270 | clone 天然对齐 |
| 图片高清直接动作语义 | 267/271 | 交叉确证 |
| 图片面板默认态（Lib Image 18 三源一致） | 265/266 | 确证 |
| 画布切换下拉结构 | 286/287 | clone 结构匹配 |
| 角色库原型清单 | 282 | 无漂移 |
| 提交路径（付费墙/账号态限制） | 275–278 | 终局记录 |
| 工具箱「使用」（无可见变化） | 284 | 账号态边界 |
| 键盘 ⌘ 系 | 292 | 见上 |

## 三、阻塞项（batch 291 清单为权威版本）

1. 素材库门后内容——需承诺书授权（用户）；
2. OmniHuman 音频满足态——需可生成账号 + 人工连线；
3. 非会员任务创建/pending——需可生成账号人工提交；
4. Style Video / 动作迁移费率——需人工菜单点击；
5. 每画布 ∨ 子菜单——需人工展开；
6. 风格库/特效库入口、工具箱使用语义——需可生成账号。

## 四、下一批次优先级建议

1. **生成历史条目化**（有历史账号下的条目结构——需账号可生成，
   与阻塞 3 联动）；
2. **抽屉交互深化**（资产管理 320px 下的 搜索/筛选/排序 交互行为
   ——结构已对齐，交互语义部分未采样）；
3. **图片高清产物终态**（与阻塞 3 联动）。

## 验收

- docs check 通过；维护集 41 项于 batch 288 全量确认后代码未变
  （batch 289–293 均为采样/文档批次），确认持续有效；源站画布
  0 残留（batch 279 清理后无画布操作）。
