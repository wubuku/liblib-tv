# Batch 258 — 音频节点连线配方进展（源站 2026-09-10）

> 状态：`PARTIAL`（音频节点创建与把手发现确证；连线完成度与满足态
> 采样未确证——面板在拖拽后失焦）。

## 已确证（配方进展）

- **音频节点创建**：画布空白处 dblclick → 添加面板 → 音频（节点 id
  `a-` 前缀；创建位置跟随 dblclick 偏移——可用于避开已有节点）；
- **把手发现**：`.react-flow__handle.source/.target` 可定位，音频节点
  target（左）/ source（右）各一，视频节点同构（坐标即把手中心）；
- OmniHuman 面板在 attempt 持久化（batch 255）下跨操作稳定保持。

## 未确证

- 拖拽连线（audio.source → video.target）后边数仍为 1（拖拽前基线
  亦为 1：首帧图→视频）——连线是否建立/是否替换了原边未分辨；拖拽
  过程丢失节点选中，面板关闭，满足态读数（音频 1/1、警示、积分）
  无法获取；
- 下一配方迭代要点：拖拽后先重选视频节点（记录其 data-id）再读面板；
  拖拽前记录边 id 集合以分辨「新增 vs 替换」。

## 验收

- docs check 通过；源站画布 **0 残留**；无代码变更。
