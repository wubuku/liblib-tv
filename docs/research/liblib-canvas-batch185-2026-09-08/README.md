# Batch 185 — Batch 172-178 新表面的移动端 390 断点核查

## 背景

batch172-178（画布右键菜单、尝试芯片+原字形图标、模型菜单滑层）均只在
桌面视口验收过。本批新增 `verify-liblib-batch185.py` 在 390×844 核查。

## 核查项（14 checks 全过）

- 尝试芯片：390 下可见、选中白 10%、原字形 svg 渲染、无页面横向溢出。
- 节点右键菜单：node 变体打开且整体在视口内（x≥-1、x+w≤391、
  y+h≤845）。
- 空白 pane 右键菜单：pane 变体、视口内、 Escape 后无横向溢出。
- 模型菜单（fit-view 后，batch22 移动相位同款准备）：视口内、行高 52、
  滑层类名保留。
- 0 console error。

## 过程要点（写入方法库）

- 390 的画布 2 预设构图铺满视口：pane 空白点必须
  `elementFromPoint` 动态探测（固定坐标会打到预设视频节点上）。
- 未 fit-view 时 660 宽卡内面板会锚定出屏（菜单 x=-200），模型菜单相位
  需先 Meta+0（batch22 移动相位同款节奏，fit 缩放 12%）。
- 预设节点 v-UGQZzZOpbv 在 390 原生缩放下不可点（视口外），fit-view 后
  可用。

## 验收

- `verify-liblib-batch185.py`：14 checks 一次通过（修复探针后）。
- 回归绿：22（移动相位）/161（移动+平板 20 checks）/151/172/173/177/
  178/21；`npm run check` 0 errors；docs check 通过。
- 不证明：源站右键菜单的移动端形态（移动端右键交互在源站未采样——
  本批仅核查 clone 侧适配）；触摸长按等移动手势。
- 无源站采样（纯 clone 核查），源站画布保持 0 残留。
