# Batch 291 — 阻塞项人工采样方案清单（供用户授权/人工操作确认）

> 状态：`PLANNING`（汇总 batch 236–290 全部阻塞与 SOURCE_UNKNOWN 项，
> 列出各所需授权/操作；无代码变更）。

## 一、需用户授权（承诺书）

| 项 | 说明 | 解锁后可采 |
|---|---|---|
| **素材库门后内容** | 素材库（真正的素材管理面，含用户上传素材库）需先签署承诺书；batch 289 确认工具栏「素材库」条目只是库快捷弹层，门后面板未进入 | 门后结构、素材上传交互、素材在画布的引用方式 |

## 二、需账号可生成（当前非会员账号无法发起生成——batch 278 终局）

| 项 | 现状 | 人工操作 |
|---|---|---|
| **OmniHuman 音频满足态** | 音频 0/1 红色缺失、连线是唯一机制、拖拽被拒（batch 257–261） | 人工连接音频节点后观察 音频 1/1、警示消失、积分变化 |
| **非会员任务创建/pending 态** | 2.0/2.5 付费墙、Lite 静默未生效（batch 275–278） | 人工确认提交路径（是否需先连素材/特定免费模型/账号充值），采样 pending 态与 batch 33 合同对照 |
| **图片高清生成结果** | 容器+AI生成徽章已采（batch 267），生成完成后的图片产物未等到 | 等待/重试一次生成，采样产物节点终态 |
| **155 vs 135 模式溢价假设** | 155 为过渡态误读已修正（batch 254） | 无需——已关闭 |

## 三、需人工点击（自动化点击不可靠/会话间歇）

| 项 | 现状 | 人工操作 |
|---|---|---|
| **Style Video / Kling3.0 动作迁移 费率** | 菜单最末两行点击不可靠（batch 243/259/272 四次尝试） | 人工点开模型菜单滚至末两行点击，读页脚积分与清晰度列表 |
| **每画布 ∨ 子菜单内容** | 自动化两次未稳定展开（batch 286/287） | 人工点击 画布 1 行尾 ∨，记录菜单项 |
| **风格库/特效库入口点击行为** | 快捷弹层重现间歇（batch 289/290） | 人工打开弹层点击 风格库/特效库，观察新增节点 |
| **工具箱「使用」生成语义** | 使用后无可见变化（batch 284，生成依赖账号） | 在可生成账号上 使用 后提交，观察预设是否生效 |

## 四、可继续自动化（无阻塞）

- 各 BLOCKED 项解除后的 clone 实施批；
- 既有表面的定期稳定性确认（batch 288 基线 41 项）。

## 验收

- docs check 通过；无源站操作；无代码变更。
