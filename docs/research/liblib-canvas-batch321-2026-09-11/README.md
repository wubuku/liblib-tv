# Batch 321 — 展示设置视图切换深采再次不确定（源站 2026-09-11）

> 状态：`INCONCLUSIVE`（展示设置菜单第三次未展开；无代码变更）。
>
> 证据：`menu.png`、`grid-view.png`（点击后无菜单，仅抽屉列表）。

## 记录

- 抽屉打开成功（两视频节点条目 + 画布/资产页签 + 工具行四件）；
- **展示设置菜单第三次未展开**（aria-label 点击后 menu: null）——
  与 batch 300/308 同模式。该菜单的展开可能需要特定前置（如先有
  分组内容）或存在会话随机性，自动化重试成本高于收益；
- batch 298 的视图布局菜单合同（列表/宫格/分组展开收起）按 batch 297
  采样维持。

## 间歇性根因假设汇总（跨 batch 290/296/300/308/321）

浮层类 UI（工具栏弹层/下拉菜单/全页覆盖层）在自动化驱动下间歇性
不展开——统一根因疑似 React 浮层的挂载时序与自动化 click 的
事件派发竞争。**弹层类表面的采样建议统一转为人工会话内连续操作。**

## 验收

- docs check 通过；源站画布 **0 残留**；无代码变更。
