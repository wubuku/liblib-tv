# Batch 327 — 抽屉条目「更多操作」单会话重采：定位失败（源站 2026-09-11）

> 状态：`INCONCLUSIVE`（定位到节点 aria-label 未找到——抽屉开启/
> 条目渲染状态跨会话不可延续；无代码变更）。
>
> 证据：`menus-log.json`、`final-log.json`。

## 记录

- 单会话内从零建状态：建视频节点 ✓ → 资产管理 点击后抽屉未开或
  条目渲染变化（定位到节点 aria-label 未命中，entry row: None）；
- 更多操作 菜单内容维持 `SOURCE_UNKNOWN`——须在源站会话内以人工
  悬停条目后点击展开；
- batch 295 已实装 clone 侧的悬停操作视觉呈现（… 菜单 + 发送图标），
  内容/行为为 clone 附加（batch 305 同类先例）。

## 方法学结论（batch 326 + 327 合并）

弹层/菜单/抽屉类交互在跨会话自动化下持续不稳定——根因为源站
React 应用的会话状态（抽屉开合/条目渲染/选中态）不跨 CDP 连接
延续。**已转入人工采样清单**（batch 291 权威版）。

## 验收

- docs check 通过；源站画布 **0 残留**；无代码变更。
