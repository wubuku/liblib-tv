# Batch 111 Plan：角色库模态几何与详情标签对齐 2026-09-05 补采样

> 状态：`IN_PROGRESS`
>
> 建档日期：2026-09-05。
>
> 上一批 checkpoint：Batch 110（`batch/110-aged-gate-deprecation`）。
>
> 源站证据：2026-09-05 补采样（`original-character-library-modal.png` + 本轮
> `getBoundingClientRect` 精测，只读切换角色）。

## 1. 采样事实（`SOURCE_FACT`，viewport 1440x900）

- 模态壳：`1304x731 @ (68,84)`（左右留 68、上 84、下 85），流式宽高。
- 内边距 33px；名字行 rel y=74；四图行 rel y=113、h=301，列宽
  `226/226/226/536`（≈1:1:1:2.37，gap 8）。
- 四图标签：`角色立绘 / 脸部近景 / 表情参考 / 三视图`（clone 已一致）。
- 说明模板：`{name}，详见角色全身图、面部特写、表情九宫格与人物呈现板。`
  （两角色一致）；`应用至画布` 按钮 109x32 右下对齐。
- 标签：`甜妹/清新少女` → `女主 女 现代 青年 温柔`；`温柔熟男/理想男友` →
  `男主 男 现代 青年 温柔`。
- 关闭按钮 aria 为 `close`。

## 2. 范围

### 包含

1. 模态壳尺寸/边距对齐（单视口采样，clamp 到更小视口）。
2. 详情区按实测几何重排（内边距/列比/高度/说明与按钮位置）。
3. 标签：两个已采样角色用精确映射；未采样角色保留启发式并修正
   `主角`→`男主/女主` 前缀规则。
4. 关闭按钮 aria 对齐 `close`。

### 不包含

- 未采样角色的真实标签、卡片条尺寸/横向滚动精确几何、`角色筛选`/`最近使用`
  下拉内容、真实图片资产。

## 3. 证据边界

`SOURCE_FACT`：上表全部；`CLONE_DECISION`：clamp 策略、未采样角色启发式；
`SOURCE_UNKNOWN`：其余 15+ 角色标签、其他视口几何。

## 4. 验证

- `scripts/verify-liblib-batch111.py`：1440x900 断言模态壳尺寸±8、四图标签、
  甜妹标签集、说明模板、应用至画布、close 关闭；零诊断。
- 复跑 batch11（overlay 互斥）；`npm run check`、`npm run docs:check`。

## 5. 完成定义

模态壳与详情区对齐采样几何；相邻回归与全量门通过；特性分支 commit/push。
