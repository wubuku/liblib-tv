# Batch 296 — 筛选菜单选项集采样受阻（源站 2026-09-10）

> 状态：`INCONCLUSIVE`（会话状态复杂化：搜索文本残留 + 抽屉关闭 +
> 活动画布切换至画布 2；无代码变更）。

## 记录

- 点击 筛选 图标失败两次：搜索开启时工具行右移导致 (60,166) 命中
  搜索框；以 aria-label 驱动的第二次尝试被抽屉关闭状态打断；
- **clone 筛选选项集（全部/图片/视频/文本/分组）为既有合同维持**；
  源站选项集 `SOURCE_UNKNOWN` —— **batch 301 已解除**：单会话采样确证
  源站筛选菜单十项与 clone 逐字一致（见
  `../liblib-canvas-batch301-2026-09-11/README.md`），无漂移；
- 附带确证：batch 295 的无匹配文案「当前画布没有相关搜索结果」在
  源站持续渲染（截图左上可见）✓。

## 方法學沉淀（抽屉状态机）

抽屉工具行随搜索开关**重排**（图标位置漂移），几何定位不可靠——
后续交互采样必须以 aria-label 驱动（aria-label 驱动版本已验证可用，
本次失败系会话态：先前的画布切换使抽屉关闭）。

## 下一迭代要点

单会话内连续完成：开抽屉 → 确认搜索关闭 → aria-label 点筛选 →
即时 dump。不跨连接。

## 验收

- docs check 通过；源站画布 0 节点（画布 2 本就为空）；无代码变更。
