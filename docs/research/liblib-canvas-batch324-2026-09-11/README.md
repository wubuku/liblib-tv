# Batch 324 — 生成历史入口深探：按钮存在但覆盖层不打开（源站 2026-09-11）

> 状态：`INCONCLUSIVE`（按钮 aria-label 定位精确命中但覆盖层三次均
> 未打开；无代码变更）。
>
> 证据：`zoom-retry-log.json`（三次尝试记录，按钮坐标稳定命中）。

## 记录

- 生成历史 按钮 aria-label 定位**三次精确命中**（坐标稳定
  [1016, 2364]），但点击后覆盖层**均未打开**（overlay False ×3）；
- 排除前批的位置漂移假设——按钮可定位但点击无效，可能为：
  面板/抽屉互斥（batch 58/65 生命周期）、需选中节点前置、或
  源站该入口当前有条件限制 `SOURCE_UNKNOWN`；
- 缩放控件采样因此维持不确定（依赖覆盖层打开）。

## 累计判定（batch 323 + 324）

生成历史覆盖层的打开条件在自动化下不可达——与 batch 321 弹层间歇性
同域。**该表面的深采转人工会话内连续操作**。

## 验收

- docs check 通过；源站画布 **0 残留**；无代码变更。
