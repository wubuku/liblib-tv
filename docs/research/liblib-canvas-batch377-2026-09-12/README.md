# Batch 377 — jimeng batch 3 GenPanel 对照（`REVIEW_RECORDED`）

> 状态：jimeng batch 3「空节点选中 + GenPanel」可视化对照完成——
> **1:1 一致**。liblib.tv 恢复重测：`RECOVERY: still-broken` 维持。
> 无代码变更。

## 对照（源站 2026-09-12 截图 vs clone batch 3 截图）

- **一致**：视频 1 节点选中态（尺寸/位置/描边）；节点下方 GenPanel
  （+ 附件钮、占位文案「上传参考图、输入文字或 @ 主体，描述你想生成
  的视频」、模型行「即梦 Seedance 2.0 VIP ✦∨ | 16:9 · 720P ✦∨ · 1∨ |
  全能参考 ∨ | 4s ∨ | @ | ✦56 | ↑ 发送」）；
- **微差**：clone 占位文案中「@ 主体」渲染为高亮芯片（源站为内联
  文本）——mock 级微差（CLONE_DECISION，并行路线可裁决）。

## 源站状态

liblib.tv：`RECOVERY: still-broken` 维持。

## 后续候选

- jimeng batch 4-8 同款对照抽查（context menu/insert menu/repaint/
  video-edit/zoom-help/infer）；
- liblib.tv 恢复后 BLOCKED_SOURCE 补采与 CLONE_DECISION 替换。
