# Batch 152 — /project 卡副行仅日期 + 覆盖矩阵刷新（源站 2026-09-07）

## 对齐项

源站 /project 项目卡文本仅「标题 + 日期」两行（实拍：未命名 / 2026-09-06），
无工作区前缀。clone 卡副行由 `{projectName} · {today}` 改为 `{today}`。

## 覆盖矩阵刷新（liblib-surface-coverage-2026-09-07）

以 Batch 149-151 的复采证据更新矩阵：

- 视频面板积分公式 → ✅（第四数据点 135 证实；Auto 因子仍两点校准）。
- 模型菜单 → 触发器已采（显示「2.0」），菜单展开 rAF 门控待窗口前置。
- /project 列表页 → 吸收 Batch 150（新开标签）与 152（副行仅日期）。
- 协作跟随横幅 → 2026-09-07 实况核对吻合。
- 尝试芯片 → Batch 128 契约保留 + 09-07 冲突/一次性门控记录，待受控复测。
- 新增「分组内嵌生成面板」行（Batch 149 结构记录）。

## 验收

- `verify-liblib-batch152.py`：7 checks 通过（日期唯一性/无工作区前缀/节点计数保留）。
- 相邻回归绿：119 / 136 / 148 / 150。
- `npm run check`：0 errors、8 warnings（既有基线）。
