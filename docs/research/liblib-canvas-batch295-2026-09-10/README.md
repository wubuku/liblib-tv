# Batch 295 — 资产抽屉搜索交互采样 + 无匹配文案对齐（源站 2026-09-10）

> 状态：`IMPLEMENTED`（交互采样 → 一处文案对齐 → 7 checks 验收 → 41 项
> 回归绿）。
>
> 证据：`search-nomatch.png`、`search-result.png`（命中态）、
> `drawer-buttons.json`（按钮图）。

## 源站事实（`SOURCE_FACT`）

- **搜索实时过滤**：键入即过滤列表（命中「节点 10」→ 仅显示 视频节点
  10）；
- **无匹配专属文案**：查询无结果时显示 **「当前画布没有相关搜索
  结果」**——区别于无节点空态文案（漂移已对齐）；
- **条目 aria-label**：主按钮 = **定位到节点**（点击条目即定位选中，
  与 clone selectNode 一致 ✓）；悬停两图标 = 更多操作 + 定位到节点
  （发送箭头亦为定位语义）；
- **源站工具行无独立排序按钮**：搜索 / 筛选：全部 / 所有评级 / 展示
  设置 四件——clone 的 ArrowDownAZ 排序为 clone 附加项（轻微超出，
  记录不删）。

## 实施（`AssetManagerPanel.tsx`）

空态文案三分支：`normalizedQuery` 非空 → **当前画布没有相关搜索
结果**；否则维持 资产页签/画布页签 原空态文案。

## 验收

- `verify-liblib-batch295.py`：**7 checks**（搜索按钮/输入框；无匹配
  专属文案 + 列表隐藏；清空恢复列表）。
- 回归绿：21–268 维护集 41 项。
- `npm run check`：0 errors（8 warnings 基线）；docs check 通过。
- 源站画布 **0 残留**。

## 不证明

- 筛选菜单的选项集与联动（菜单已打开截图 `filter-menu.png`，
  内容采样排后续）；
- 源站排序缺失是否真实（可能在 展示设置 菜单内）。
