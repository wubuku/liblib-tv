# Batch 309 — 带内容画布的 工作流/故事板 切换：无操作确认 + 添加面板条目漂移发现（源站 2026-09-11）

> 状态：`SAMPLING_RECORDED`（否定性确证 + 方法发现；无代码变更）。
>
> 证据：`before-workflow.png`、`after-workflow.png`、`restored.png`、
> `workflow-log.json`。

## 源站事实（`SOURCE_FACT`）

1. **工作流/故事板 切换在带内容画布上无任何可见效果**：音频+图片+
   视频三节点画布上，切换前后节点布局/边/视口 transform **逐字节
   一致**（三快照）——该切换为无操作或保留态功能；
2. **添加面板条目位置随每次打开漂移**：按文本匹配点「视频」有时命中
   音频/图片 条目——本次意外建成 音频+图片+视频 混合画布，反向解开了
   batch 302/303 的「第二次创建失败」之谜（面板重开位置漂移所致，
   非面板缺陷）；
3. 音频节点确认渲染：波形图 + 00:00/00:03 时长 + 播放键（音频节点
   形态首采）；
4. 图片节点确认 AI生成 徽章 + 1080×1446 尺寸徽章（与 batch 268
   实现一致）。

## Clone 对照

- 工作流/故事板 切换：clone 为 editorMode 切换（workbench/storyboard）
  ——语义按 clone 合同维持（源站该切换当前为无操作，不需对齐动作）；
- 添加面板条目漂移：clone 面板为固定定位条目，无此问题 ✓。

## 方法学沉淀

添加面板条目点击**必须按 aria-label 或相对面板的位置驱动**，禁止
跨打开状态复用文本匹配坐标。

## 验收

- docs check 通过；源站画布 **0 残留**；无代码变更。
