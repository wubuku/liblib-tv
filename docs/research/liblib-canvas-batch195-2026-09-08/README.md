# Batch 195 — 特效库锚定规则闭环（静态屏幕定位，portal 实装）

## 三态采样（源站 2026-09-08；`source-gallery-anchor-states.json` 存档）

- 将节点拖至上/中/下三态，逐态开特效库：**特效库盒子完全相同**
  `[177, 444, 1567, 235]`——不随触发器位置移动。
- 结论：**静态屏幕定位**（水平居中：177×2+1567≈1920；垂直 top≈444，
  在 854/826 两种视口高度下一致）。Batch 194 的「状态依赖锚点」结论
  被推翻——当时的 y≈912 是定位器误匹配（脚本伪影）。

## 实施

- clone 特效库锚定由 `fixed bottom-[175px]` 改为 `fixed top-[444px]`
  水平居中。
- **关键修复：`createPortal` 到 document.body**。特效库原先渲染在
  React Flow 变换子树内，`position: fixed` 被变换祖先劫持（实测
  y=923 随流变换缩放偏移），portal 后回到真实屏幕坐标（y=444 ✓、
  x 居中 ✓）。
- 验证器关闭机制改用 footer 合成 mousedown（pane 点击会取消节点选择
  导致面板卸载）。

## 验收

- `verify-liblib-batch195.py`：8 checks（两次开库 top≈444/水平居中/
  位置稳定）。
- 回归绿：191 / 192 / 193 / 172 / 178 / 185 / 186 / 187 / 188 / 189。
- `npm run check`：0 errors（8 warnings 基线）；docs check 通过。
- 方法沉淀：clone 内画布坐标子树里的「fixed」浮层必须 portal 到
  body 才能获得真实屏幕坐标。
- 不证明：top 444 的规则来源（固定值/容器锚定——两种视口高度下一致，
  按固定值实现）。
- 源站测试残留清理：0 残留（本批无新增源站节点）。
