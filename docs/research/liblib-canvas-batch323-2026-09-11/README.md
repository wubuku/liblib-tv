# Batch 323 — 生成历史缩放控件采样受阻（源站 2026-09-11）

> 状态：`INCONCLUSIVE`（生成历史覆盖层未打开——工具栏按钮位置漂移；
> 无代码变更）。
>
> 证据：`history-open.png`（点击后无覆盖层）。

## 记录

- 生成历史 按钮坐标点击（1016, 2364）后覆盖层未出现——底部工具栏
  按钮位置随抽屉/面板状态漂移（batch 264 的 320px 抽屉与 工具箱
  浮层均会影响底栏布局）；
- clone 的 HistoryPanel 缩放控件（batch 101 zoom 合同，50–150 range）
  为既有实现，源站对应控件形态待覆盖层成功打开后采样。

## 方法学备注

底部工具栏按钮坐标不跨状态稳定——探针需按 aria-label 实时定位
（本批已用，但按钮坐标在点击后仍漂移，需点击前实时重查）。

## 验收

- docs check 通过；源站画布 **0 残留**（两视频节点清至 0）；无代码
  变更。
