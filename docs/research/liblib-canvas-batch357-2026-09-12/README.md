# Batch 357 — batch 89 定案：遮罩点击命中被面板覆盖（`VERIFIER_FIXED`）

> 状态：batch 89 从 sweep 失败恢复**确定性三连绿**；台账 §5.z3
> 更新（89 → 已修复转绿）。无产品代码变更。源站恢复探测：
> `RECOVERY: still-broken`。

## 深查结论（三层递进）

1. 关闭按钮（全屏遮罩 inset-0 z-20）在面板打开后可见 ✓（0.5s 实测）；
2. 自动关闭窗口约 0.5~1.5s（焦点收纳）——中间的常规等待会把关闭
   点击挤出窗口；
3. **真正死因**：关闭遮罩中心 (195, 400+) 被 z-30 场景对象面板
   （220px 宽）覆盖——点击遮罩中心命中面板子树，playwright 重试
   直到超时。真实用户点的是遮罩右侧暗色区（面板外），不受影响。

## 修复（验证器）

移动段重排为紧凑序列：开面板 → add-camera 可见性（2s 超时）→
溢出检查 → **点遮罩右侧空白区 (310, 400)**（timeout 2s）。三连跑
确定性全绿。

## AGED_GATE 清算进度（**10/13 已转绿**）

已转绿：29（338）、39（346）、40（347）、41（348）、46（349）、
48（353）、49（354）、57（355）、64（351）、**89（357）**。
维持归档：6（marquee 历史化，AGENTS.md 在档）、
44（考古定案：append 纯拼接自始如此）、61（语料断言，356 已修
实质部分——注意 61 已于 356 转绿）。

**更正**：61 已于 batch 356 转绿。13 项中仅 6（marquee，历史化）
与 44（考古定案，预设语义待产品裁决）维持非绿——且两者均有
明确归档理由，不属于回归。

## 验收

- batch 89 三连跑全绿；docs check 通过（917 Markdown）；
- 无产品代码变更。

## 后续候选

- 源站恢复后 BLOCKED_SOURCE 补采与 CLONE_DECISION 替换；
- 相机运动预设 append 语义的产品裁决（batch 352 记录）。
