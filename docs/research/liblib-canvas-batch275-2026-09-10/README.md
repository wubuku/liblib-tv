# Batch 275 — 提交后会员付费墙发现（源站 2026-09-10 直采）

> 状态：`SAMPLING_RECORDED`（会员门为账号态边界，clone 不实施——
> batch 12 边界「不连接真实账户」一致）。
>
> 证据：`post-submit-2.png`（付费墙全页）、`submit-log.json`。

## 源站事实（`SOURCE_FACT`）

视频面板提交（生成 ↑ 按钮，提示词已填、模型 2.0——项目记忆的会员专属
模型）：

- **不创建任务**，弹出**会员付费墙全页**：顶部横幅「年会员 Seedance
  2.5 生成限时折扣」+ 主标语「**正在使用会员专属模型，升级会员继续
  使用**」；
- 定价：标准版 569/年 → 进阶 1199 → 高级 3999 → 豪华 6999 → 至尊
  10299/年；并发任务档位 8/12/20/无限；云存储 20GB→500GB；
- **每月生成数量配额表**（模型 × 档位）：Seedance 2.5 720P
  33秒→1,435秒、Seedance 2.0 56秒→2,444秒、Happy Horse 1.0
  63秒→2,750秒、Kling 5.0 108秒→6,250秒、Lib Image 750→33,000张、
  General Image Pro 107→4,714张；
- 积分余额 100 未扣（任务未创建）。

## 判定与边界

- **模型分级为会员门**：非会员提交会员专属模型（2.0 VIP 系）→ 付费墙
  拦截；这解释了历史采样中模型可用但提交行为未验的空白；
- clone 不实施会员门（原型边界，与 batch 12「不连接真实账户」一致）；
  clone 的提交直通 pending 节点合同（batch 33）保持不变；
- 非会员可用的 2.5 提交流（或免费模型提交）仍未采样——后续若采样，
  需先把模型切回 2.5。

## 方法学备注

- 源站提示词输入区为富文本容器（非 textarea），定位需按占位文本
  搜索 + 坐标点击；
- 会员弹窗以 Escape + 节点清理恢复。

## 验收

- docs check 通过；源站画布 **0 残留**（弹窗已关、测试节点已删）；
  无代码变更。
