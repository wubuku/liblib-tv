# Batch 156 — batch93 移动端抽屉关闭点击时序 flake 加固

## 根因

移动端（390×844）抽屉关闭按钮 `关闭移动端面板` 是 `absolute inset-0` 的全屏
遮罩，Playwright 默认点元素中心（x=195）：左侧场景对象抽屉 `w-[220px]`
覆盖 0..220 → 中心点落在抽屉内；当树行恰好滑到该 y 时树行拦截点击 → 30s
超时。属性抽屉在右侧 `w-72`（x≥102），同样会拦截中心点。拦截与否取决于
200ms 滑入动画与行内容位置 —— 间歇性 flake（Batch 154 扫描中首跑失败、
复跑通过）。

## 加固

- 场景对象阶段：遮罩点击点取 `position={x:320, y:400}`（左侧抽屉之外）。
- 属性阶段：点击点取 `position={x:56, y:400}`（右侧抽屉之外）。

## 验收

- `verify-liblib-batch93.py` 连续 3 次通过。
- 导演台相邻回归绿：36 / 43 / 77。
- 无产品代码变更（仅验证器等待/点位加固）。
