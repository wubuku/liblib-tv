# Batch 300 — 展示设置视图切换深采样受阻（源站 2026-09-11）

> 状态：`INCONCLUSIVE`（展示设置菜单未重现；无代码变更）。

## 记录

- aria-label 驱动的 展示设置 点击本次未展开菜单（menu: null）——
  弹层/菜单类交互的会话间歇性再次出现（同 batch 290 模式）；
- 附带确证：抽屉条目列表正常（视频节点 12 单条 + 所有评级过滤可见），
  batch 264/281 合同持续有效；
- clone 侧 batch 298 的视图布局菜单合同（列表/宫格/分组展开收起）
  按既有采样维持，源站行为深采样待会话稳定后重试。

## 方法学备注

菜单类弹层在 aria-label 点击后有时不展开——与 batch 290 素材库弹层
同模式。推测与浮层挂载时序/焦点相关，建议后续探针在点击后先枚举
全 DOM 再判定（本轮 menu 探测已含，仍 null——点击本身未生效）。

## 验收

- docs check 通过；源站画布 **0 残留**；无代码变更。
