# Batch 251 — 首尾帧生成视频芯片流采样（源站 2026-09-09 直采）

> 状态：`SAMPLING_RECORDED`（实施留 batch 252）。
>
> 证据：`a-firstlast.png`（节点结构+面板全貌）、`b-reselect.png`、
> `sample-log.json`。

## 源站事实（`SOURCE_FACT`，a-firstlast.png 截图直证）

点击视频节点「首尾帧生成视频」尝试芯片后：

1. **自动创建两个图片节点**：上 = 首帧（青柠气泡水罐特写）、下 = 尾帧
   （短发女生喝同款水）；各一条**曲线边**汇入视频节点左侧同一把手
   （两线入口交汇，蓝色流动效果）；
2. **面板**：完整 5 pill；**两个参考槽**并排（48×55，角标 1/1）；
   **AI 生成的过渡提示词**预填（「手从白帆布包中取出青柠气泡水罐的
   特写，过渡到短发女生在户外草地仰头喝同款气泡水的中景，日系清新
   治愈风格，明亮自然光，柔和暖调，胶片质感，动作流畅自然」）；
3. 页脚：**2.0 💎 / 全能参考 / 16:9 · 720P · 5s · 1个 / ⚡155**——
   首尾帧流把模型切到 **2.0**（首帧流保持 2.5）；提示词为**有内容**
   状态（非占位符）。

## 数据点（`SOURCE_DATAPOINT`，未受控）

- 2.0 · 全能参考 · 16:9 · 720P · 5s · 1个 = **155**：高于 2.0 已知
  文生视频 5s 读数 135（27/s）——疑 全能参考模式 在 2.0 上有溢价
  （155/135≈1.15）或读数含其它未控因素；与 2.5 的 全能参考=文生视频
  同价（230/690 双证）行为不一致，需受控复测。

## 与首帧流的差异小结

| 维度 | 首帧生成视频（batch 237/239） | 首尾帧生成视频（本批） |
|---|---|---|
| 自动创建 | 1 个图片节点 | **2 个图片节点** |
| 边 | 1 条直线 | 2 条曲线（同把手交汇） |
| 模型 | 保持 2.5 | **切到 2.0** |
| 模式 | 全能参考 | 全能参考 |
| 参考槽 | 1 个 | 2 个 |
| 提示词 | 说明文案（无输入框） | **AI 过渡文案预填**（有内容） |
| 积分 | 230（2.5·Auto·5s） | 155（2.0·16:9·5s） |

## 实施建议（batch 252 候选）

clone 可按观察值受控实施：创建两个图片节点（上下堆叠于视频节点左侧）
+ 两条边 + 双参考槽 + 提示词预填（用采样的 AI 文案作样例文本）+ 模型
切 2.0 + 全能参考 + 16:9·720P·5s。曲线边形态受「不改 LibTV 边流效果」
约束——需先核对 clone 边类型是否已有 bezier 变体。

## 采样缺口

- 去选重选后芯片是否恢复：本轮因流程混杂未获干净读数（重选面板为
  空读——探针误用 clone 选择器所致，源站面板需文本/几何探针）；
- g- 前缀节点（本轮图中出现 1 个）身份未确认；
- 155 读数的受控复测（模式溢价假设）。
