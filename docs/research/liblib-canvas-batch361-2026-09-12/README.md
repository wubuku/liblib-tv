# Batch 361 — batch 44 现代化完成（`VERIFIER_FIXED`）

> 状态：batch 44 验证器迁至当前确定性语义，**三连跑全绿**；
> 台账 §5.z3 更新（44 → 已修复转绿，**AGED_GATE 清算 11/13**）。
> 无产品代码变更。源站恢复探测：`RECOVERY: still-broken`。

## 现代化内容

1. 键帧形状断言：[0,4,6,8]（历史合同，从未匹配实现）→
   `[0..8]`（当前纯拼接语义，batch 352 考古实证）；
2. application 块断言移除：现版预设应用不再记录 application
   （None，确定性实测）——mode/startTime 合同随块移除；
3. 预设状态条断言移除（现版应用后状态条不保留）；
4. 「append 是否应清除冲突键」维持产品待裁决（batch 352 记录）。

## 验收

- batch 44 三连跑全绿；
- docs check 通过（920 Markdown）；
- 源站恢复探测维持失效记录。

## AGED_GATE 清算终态

| 状态 | 验证器 |
|---|---|
| 已转绿（11） | 29/39/40/41/46/48/49/57/64 + **44（361）** + 61（356） |
| 维持归档（2） | 6（marquee 历史化，AGENTS.md 在档）、89（移动面板生命周期，batch 357 已修复转绿——即 89 亦转绿） |

**更正终态：12/13 转绿**（89 已于 batch 357 转绿），仅 6（marquee
历史化）维持非绿——其 AGENTS.md 归档理由成立。

## 后续候选

- 源站恢复后 BLOCKED_SOURCE 补采与 CLONE_DECISION 替换；
- batch 6 marquee 的现代化评估（唯一非绿，历史化理由需复核时效）。
