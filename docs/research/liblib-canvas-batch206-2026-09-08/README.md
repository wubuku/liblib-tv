# Batch 206 — 空画布快捷芯片点击行为采样（证据批）

## 源站事实（窗口 rAF ~31fps；`source-chip-click.json` / `source-scriptv2-pair.json` 存档）

- 空画布快捷芯片集合**随会话变化**：一次为 4 枚（开 Agent 抽屉时），
  一次仅 2 枚（故事脚本生成 / 角色三视图）。
- 点击「故事脚本生成」芯片 = **一次创建两个节点**：
  1. `text` 节点（350×200），预填完整「剧本」markdown 内容；
  2. **`script-v2` 节点**（350×350，「脚本生成器」）——源站新节点类型，
     clone 尚未实现。
- 两节点间无连线（edges: 0）。

## 处置

- 证据批：`script-v2` 为大表面（新节点类型 + 编辑器 + 面板），另立
  批次实现；本批仅记录结构与行为。
- clone 现状对照：CanvasEmptyState 快捷芯片（Batch 118）点击创建单节点
  ——与源站的成对创建存在分歧，记录待迁移。
- 源站测试残留清理：采样节点已删（清理过程中发现 text 节点需先 Escape
  退出编辑态再 Backspace 才可删；右键菜单删除对编辑态节点无效）。

## 验收

- 证据 JSON/截图存档；源站画布 0 残留。
- 无 clone 代码改动（`npm run check` 基线不变）。
- 不证明：script-v2 节点的完整编辑器/面板；芯片集合变化的规则；
  全能参考视频/音频生成视频芯片的行为。
