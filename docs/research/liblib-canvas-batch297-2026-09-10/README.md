# Batch 297 — 抽屉「展示设置」菜单全量采样（源站 2026-09-10 直采）

> 状态：`SAMPLING_RECORDED`（菜单四项全采；对照结论如下；无代码变更）。
>
> 证据：`display-menu.png`、`sample-log.json`（180×167 菜单 @ 280,186）。

## 源站事实（`SOURCE_FACT`）

「展示设置」菜单（aria-label 前缀 展示设置，180×167）四项：

1. **列表展示**；
2. **宫格展示**；
3. **展开全部分组**；
4. **收起全部分组**。

## 对照判定：clone 有实质漂移

clone 的同名按钮（batch 204 合同）弹出的是**类型筛选菜单**（全部/
文本/图片/视频/智能剪辑/导演台/逐帧拉片/音频/脚本/脚本旧版 十项），
而源站「展示设置」是**视图布局菜单**（列表/宫格 + 分组展开/收起）——
语义完全不同。batch 204 的直采应为当时把两个菜单混淆了（或源站
此后重构）。

## 实施排程建议

- **保留** clone 现类型筛选菜单（其内容为 batch 204 独立采样，仍为
  有效表面——可能挂在其它入口）；
- **新增** 展示设置菜单（列表/宫格/展开分组/收起分组）——需先采样
  宫格展示态与分组展开/收起行为，规模中等。

## 验收

- docs check 通过；源站画布 **0 残留**；无代码变更。
