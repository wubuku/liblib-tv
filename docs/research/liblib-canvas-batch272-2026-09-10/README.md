# Batch 272 — CDP 原生事件配方尝试：Style Video / 动作迁移维持 BLOCKED（源站 2026-09-10）

> 状态：`BLOCKED_AUTOMATION`（维持定论；CDP 配方已实现但本轮未有效
> 执行——探针簿记错误，脚本存档待复用）。

## 本轮记录

- **Style Video**：模型菜单内行定位成功（[986, 2019.5, 360×52]），
  Playwright 点击后页脚仍为长视频态（14700 / 触发器读数被
  「5分钟超长视频」芯片文本污染——探针 finder 再次误配，未切换）；
- **Kling3.0 动作迁移**：行未找到（菜单打开状态依赖前一步，级联
  失败）；
- **CDP `Input.dispatchMouseEvent` 原生点击代码路径已实现**（脚本
  `sample-log.json` 同目录批注，本 generator 版本未存档）——但本轮
  因触发器 finder 误配未执行到该分支。

## 判定

Style Video 与 Kling3.0 动作迁移**维持 BLOCKED_AUTOMATION**。四次
跨批次尝试（batch 243 / 259 / 261 类比 / 本批）的失败模式一致：
菜单最末两行的行点击在自动化下不可靠，且长视频态面板的触发器探针
易被芯片文本误配。剩余路径：人工采样，或以 store 层注入为主的
重构配方（超出当前批次合理范围）。

## 验收

- docs check 通过；源站画布 **0 残留**；无代码变更。
