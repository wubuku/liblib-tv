# Batch 158 — 默认模型回落 2.5 + 早期结论勘误（源站 2026-09-07 第四轮）

## 受控复测（窗口 visible 后重探）

页面 reload 后重选节点，拿到此前被阻塞的受控样本：

1. **batch128 联动契约直证**：新建节点点击「5分钟超长视频」芯片 →
   `2.5 / Auto · 720P · 300s · 1个`。Batch 128 的 Auto+300s 联动与模型 2.5
   在源站受控复现；Batch 151 记录的「16:9 冲突」实为预设承载节点自带配置，
   与芯片联动无关。
2. **新建节点默认模型 = 2.5**；尝试已选节点（第三轮样本）也是 2.5。
   Batch 149 依据的「2.0」仅为预设承载节点（Seedance 2.0 预设）的专属配置。

## 勘误（对早期记录）

- **Batch 153「新建节点无面板」有误**：是采样时机问题（选择尚未完成）。
  reload 后新建节点选中 → 节点卡内出现 尝试： 建议列（3 个 36px pill）+
  卡下方独立浮层生成面板（659×190+，参考工具行/提示词/页脚/高级设置）。
- **Batch 151「尝试一次性门控」有误**：芯片选择后仍留在节点卡内，与面板共存。
- **Batch 149 默认模型 2.0 过度泛化**：本 batch 回落 2.5（两个直接样本 vs
  一个预设专属样本）。触发器缩写显示规则（去 ` VIP` 后缀）保留。

## Clone 变更

- 默认模型（非续写）`2.0 VIP` → `2.5`；验证器 batch149（触发器/菜单选中态）、
  batch22（boot 选中项/描述/行高）、batch33（过程快照 model）同步回落迁移。
- 其余 Batch 149 视觉对齐（纵向开关列/引用槽/工具行/积分块）不受影响。

## 待办修订

- 「尝试芯片移入节点卡内（源站位置）」成为新的对齐候选（现为面板内横排，
  源站为卡内纵排 36px）—— 需要 VideoNode 卡片布局改造，另开 batch。

## 验收

- 相邻回归绿：149 / 22 / 33 / 21 / 128 / 155 / 125。
- `npm run check`：0 errors、8 warnings（既有基线）。
