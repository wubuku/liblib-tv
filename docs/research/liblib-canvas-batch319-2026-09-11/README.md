# Batch 319 — Style Video 禁用态视觉铁证 + 定论维持（源站 2026-09-11 直采）

> 状态：`ROOT_CAUSE_VISUALLY_CONFIRMED`（无代码变更）。
>
> 证据：`StyleVideo.png`（灰色禁用行 + 拒绝 toast 同框铁证）。

## 定论（`SOURCE_FACT`）

菜单中 **Style Video 行呈灰色禁用态**（整体降透明度、不可点击），
旁附拒绝 toast「该模型不支持当前选入的音频素材」——附挂音频素材时，
不支持音频的模型行**直接禁用**。点击从未生效的原因非自动化方法问题，
而是行级 disabled 状态。

- Kling3.0 动作迁移（同为需音频素材模型）行为一致（batch 318 已证）；
- **两模型费率采样需先移除音频素材**——音频素材连线成为新阻塞依赖
  （BLOCKED_MANUAL，batch 291 清单更新）；
- clone 无音频素材连线机制，此禁用场景 clone 不可达——无需对齐。

## 模型覆盖率最终状态

35 模型中 **33 项已有费率/数据点**；Style Video 与 Kling3.0 动作迁移
维持 BLOCKED_MANUAL（需先无音频素材环境）。

## 方法学沉淀

- 菜单行点击失败时先截图行状态——**禁用态行与可点态行视觉可辨**
  （灰色 vs 正常）；
- reload-then-clean 规则 + 标题栏点击删除配方稳定可用。

## 验收

- docs check 通过；源站画布 **0 残留**；无代码变更。
