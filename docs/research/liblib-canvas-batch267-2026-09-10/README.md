# Batch 267 — 图片高清芯片定性：一键预设生成动作（源站 2026-09-10 直采）

> 状态：`SAMPLING_RECORDED`（消歧探针决定性；无代码变更）。
>
> 证据：`disambig-800ms.png`（脱选瞬间）、`disambig-late.png`
> （生成结果全貌）。

## 定论（`SOURCE_FACT`）

「图片高清」芯片 = **一键预设生成动作**，非模式提交：

1. 点击后**源图片节点立即脱选**（selected 0，800ms 内）；
2. 生成一个 **「预设 - 图片高清」容器节点**，内含新的图片节点
   （图片节点 39，308×537 竖版）带 **AI生成** 徽章——实时 AI 生成
   （截图中人像即本次生成产物）；
3. 新节点带输出边（右侧延伸）。

## 与图生图的语义对比

- 图生图：**模式提交**（面板进入尝试态，参数可控，积分 18）；
- 图片高清：**直接生成动作**（无面板态、脱选、生成容器节点）。

## 实施排程建议

clone 的 ImageNode 尝试行如需对齐：图片高清点击 → 创建
「预设 - 图片高清」容器（含 AI生成 徽章图片节点）+ 输出边 +
源节点脱选。规模中等（容器节点 + 生成占位态）。

## 验收

- docs check 通过；源站画布 **0 残留**；无代码变更。
