# Batch 349 — batch 46 定案：截图条目命名合同漂移（`VERIFIER_FIXED`）

> 状态：batch 46 从 sweep 失败恢复**确定性三连绿**；台账 §5.z3
> 更新（46 → 已修复转绿）。无产品代码变更。源站恢复探测：
> `RECOVERY: still-broken`。

## 深查结论（逐层转储实证）

「抖动链」假设被推翻——移除插桩噪声后，失败点**确定性**落在截图
条目按钮命名断言：

- 现版条目按钮可访问名（aria-label 直证）：
  `选择截图 机位01 · 对峙中景 · 镜头-截图02` /
  `查看截图 …` / `发送到画布 …` / `删除截图 …`——
  命名带 **「 · 镜头」组段**；
- 旧验证器期望 `选择截图 机位01 · 对峙中景-截图02`（旧式连字符
  命名，无 · 镜头 段）——**命名合同漂移**，某后期批次为条目名
  增加了组段后未迁移本验证器；
- 组属性 `data-director-capture-group="机位01 · 对峙中景"` 与
  选中态 `item-selected='true'` 计数均正常。

## 修复（验证器迁移）

两处按钮名断言迁至现版命名（` · 镜头-截图NN`），移除插桩。
三连跑确定性全绿。

## AGED_GATE 清算进度（batch 335 sweep 的 13 项失败已清算 5 项）

| 状态 | 验证器 | 修复批次 |
|---|---|---|
| 已转绿 | 29（播放器菜单 Portal 化） | batch 338 |
| 已转绿 | 39（时序轮询） | batch 346 |
| 已转绿 | 40（Chrome 147 duration hack + 阈值校准） | batch 347 |
| 已转绿 | 41（authored 层迁移） | batch 348 |
| 已转绿 | 46（命名迁移） | batch 349 |
| 维持归档 | 6、44、48、49、57、61、64、89 | —（多为其合同已被 current gates 取代的历史批次） |

## 后续候选

- 源站恢复后 BLOCKED_SOURCE 补采与 CLONE_DECISION 替换；
- 其余 8 项归档失败按同一方法论复核（低优先，多为历史合同）。
