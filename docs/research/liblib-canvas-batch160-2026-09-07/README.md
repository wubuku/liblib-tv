# Batch 160 — 芯片选长视频模式 + 引用槽空态 + 去新功能条（源站 2026-09-07 整面板实拍）

## 源站事实（新建节点整面板 DOM，全面板首次完整入视野）

1. **5分钟超长视频芯片选中的是整个长视频模式**：页脚模式触发器显示
   「超长视频」、积分 **14700** = 300 × 49（长视频公式）。不只是 Auto+300s。
2. **无引用时不渲染引用槽行**：新建节点面板为 工具行 → 提示词 直连，
   没有槽行，也没有 AutoLink 汇总。
3. **面板内没有「新功能」条**（Batch 125 时代采样到的条目在 2026-09-07
   面板中不存在，移除）。

## 实施

- 芯片联动升级（batch128/155 契约保持：Auto·300s 标签不变）：
  5分钟超长视频 → `setMode("long-video")` + Auto + 300s（积分 14700 与源站
  一致）；取消 → 回 omnireference（文生视频）+ 时长钳制 ≤30（CLONE_DECISION）。
- 引用槽行改为 `references.length > 0` 条件渲染（clone 演示草稿仍带 3 个引用，
  非空态不变；空态行为对齐源站）。
- 移除 `data-video-new-feature` 条；batch125 断言迁移为「不存在」。
- batch21/22 菜单 y 偏移再迁移 -24（面板去掉 24px 条目）。

## 验收

- `verify-liblib-batch160.py`：9 checks 通过（去条/槽非空态保留/芯片→超长视频
  模式 + 14700 + Auto·300s/取消→文生视频 + 钳制）。
- 相邻回归绿：21 / 22 / 26 / 33 / 125 / 128 / 149 / 155。
- `npm run check`：0 errors、8 warnings（既有基线）。
