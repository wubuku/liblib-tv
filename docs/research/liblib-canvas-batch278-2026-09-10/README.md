# Batch 278 — 提交深度复核终局：账号态限制确认（源站 2026-09-10）

> 状态：`SAMPLING_FINAL`（提交路径探索穷尽；无代码变更）。
>
> 证据：`post-cdp-1.png`、`post-cdp-2.png`（提交后完整状态）、
> `probe-log.json`。

## 终局事实（`SOURCE_FACT`）

最完备提交条件下仍不产生任务：

- 首帧参考已连入（图片节点 → 视频节点边）；
- 提示词已填（点击输入区后键入，与首帧说明合并为
  「以当前图为首帧生成视频。一只橘猫在窗台上打盹，阳光温暖」）；
- 发送按钮 **elementFromPoint 命中验证通过**（hitIsInside=true、
  disabled=false），CDP `Input.dispatchMouseEvent` 原生点击；
- 模型 Seedance 1.0（免费档，积分 30）。

结果：无任务、无 pending 态、无错误提示——视频节点保持空占位。

## 判定

结合 batch 275（2.0 付费墙）/ 276（2.5 付费墙）：该非会员账号在
**所有采样模型上均无法发起视频生成**——账号态限制（免费积分耗尽或
生成全面转会员制），非 UI 流问题。batch 33 pending 合同的源站对照
以当时（生成可用期）观察为准，维持有效。

## 附带采样

- Seedance 1.0 态源站工具行仅 **标记 / 流域** 两 pill（pill 集分布
  第三变体——非 clone 表项，模型未入 clone 目录）；
- 首帧说明与手输提示词在输入区**顺序拼接**。

## 验收

- docs check 通过；源站画布 **0 残留**；无代码变更。
