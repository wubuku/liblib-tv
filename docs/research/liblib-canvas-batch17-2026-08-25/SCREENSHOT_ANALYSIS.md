# Batch 17 截图分析台账

> 识图日期：2026-08-25
> 用途：固化资产管理抽屉的项目/画布上下文、浏览控件和节点层级；后续不重复识别同一截图。

## 原站证据

文件：`docs/design-references/liblib-original-asset-manager-2026-08-25.png`

- 视口：`929x874`。
- 左侧抽屉从顶部到底部占满视口，宽约 `240px`，画布区域从 `x≈240` 开始。
- 左上 Logo 位于抽屉内视觉区域；其下是 `未命名项目 | 画布 2`，画布名带下箭头。
- 工作台/分镜 mode 控件位于抽屉右侧约 `x=264`，没有压在抽屉内部。
- 项目/画布行下方是 `画布 / 资产` 两个页签，右侧有一个图标命令。
- 列表工具行包含：
  - `画布元素`
  - 一个排序图标
  - `全部` 下拉
  - 搜索图标
- 列表按以下可见顺序展示 10 个节点：
  1. 分镜 #2
  2. 脚本生成器
  3. 剧本
  4. 咖啡
  5. 图片4
  6. `image_2026-06-15T11-...`
  7. `image_2026-06-15T11-...`
  8. 分镜图组
  9. 视频组
  10. 视频组下缩进的 `分镜视频-#9`
- 两个组行使用文件夹图标和展开箭头；失败视频 child 明显缩进。
- 底部右侧显示 `共 10 节点`。

## 旧 clone 截图

文件：`docs/design-references/liblib-clone-batch12-asset-manager-desktop-929-2026-08-25.png`

- 该截图在 Batch 12 流程结束后已关闭抽屉并选中图片节点，因此不能证明抽屉最终视觉。
- Batch 12 的文字台账只证明 tab 数量、节点数量和 selection，不证明项目上下文、工具行或层级已经复刻。
- 后续不得把该截图当作“资产管理已经忠实完成”的证据。

## 证据边界

### Source fact

- 抽屉几何、项目/画布上下文、mode 避让、页签、工具行、10 节点顺序和视频 child 缩进由截图直接支持。

### 未确认区域

- `资产` 页签内容没有在本截图中展开。
- 排序、筛选和搜索的展开/输入状态没有保存截图。
- 右侧页签命令图标的具体行为未确认。

## 复用规则

- 后续修改资产管理先读本文、Batch 12 台账和 `ASSET_CONTEXT_TREE.spec.md`。
- 新截图只采样本批新增的抽屉默认态、筛选/搜索态和空画布态。

## 本批 clone 验证截图

以下四张截图已通过单次 contact sheet 识别；它们是 clone 结果，不是新的原站事实：

### Default tree

`liblib-clone-batch17-asset-tree-desktop-929-2026-08-25.png`

- 项目/`画布 2` 上下文位于 Logo 下方。
- mode 控件已移动到抽屉右侧。
- 列表按原站可见顺序显示 10 项。
- 两个组行使用展开箭头，失败视频缩进在视频组下。
- 画布背景和边仍在抽屉右侧可见。

### Search

`liblib-clone-batch17-asset-search-desktop-929-2026-08-25.png`

- 搜索输入位于工具行下方。
- 输入 `分镜视频` 后只显示一个结果。
- 父组未命中时，child 以 depth 0 呈现，避免孤立缩进。

### Empty canvas

`liblib-clone-batch17-asset-empty-desktop-929-2026-08-25.png`

- 上下文更新为 `画布 1`。
- 空画布区域没有残留 `画布 2` 文案。
- 截图处于资产 tab，显示 `当前画布暂无媒体资产`；专项 DOM 同时验证画布 tab 的 `当前画布暂无节点`。

### Mobile

`liblib-clone-batch17-asset-tree-mobile-390-2026-08-25.png`

- 240px 抽屉在 390px 视口完整显示项目、画布、工具行和层级树。
- 剩余画布区域仍可见，底部工具条保持可操作。
- 没有页面级横向溢出。
