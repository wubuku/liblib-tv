# Batch 194 — 素材前置建立尝试与特效库锚点新发现（证据批，零代码改动）

## 尝试与发现（源站 2026-09-08；`source-pill-material-precondition.json` / 截图存档）

- 目标：以特效卡点击建立素材前置（batch192 流程）后采样参考/标记。
- 结果：前置未建立——特效库的卡片定位命中 (1632, 912)，**在视口
  （854px）之外**；nodes=1、edges=0，参考/标记采样仍为惰性。
- **新发现：源站特效库的锚点是状态依赖的**——昨日（191 批）在触发器
  上方（444-679）；本次开到触发器下方（约 800+，视口外）。clone 的
  `fixed bottom-[175px] left-1/2` 居中锚定是简化实现，与源站的动态锚定
  存在分歧，记录为开放问题（锚定规则——视口高度？触发器位置？——未采样
  充分）。
- 参考/标记惰性在所有成功采样的状态下持续成立。

## 处置

- 零代码改动：clone 的特效库锚定、参考/标记占位均维持现状；
  batch191/193 的现行合同复验绿。
- 不证明：素材前置的可靠建立路径（特效库卡片定位需随锚点状态自适应）；
  参考/标记的真实前置条件；源站特效库的锚定规则。
- 源站测试残留清理：采样节点已删（0 残留）。
