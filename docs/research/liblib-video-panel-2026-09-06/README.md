# LibTV 视频生成面板改版采样 — 2026-09-06

> 状态:`SAMPLING_RECORDED`(测试项目内添加视频节点并打开生成面板;未点击生成)。
>
> 环境:外部 Chromium for Testing 147,登录态,viewport 1920。
> 证据:`node-panels/video-node.png`、`s11-video-panel.png`、`s11-video-panel-full.png`。

## 面板事实(`SOURCE_FACT`)

选中视频节点后的下方面板(改版,与 2026-08-25 基线差异巨大):

- **尝试行(新)**:`尝试：` + 三个模式芯片 `5分钟超长视频 / 首尾帧生成视频 / 首帧生成视频`。
- **工具行(新)**:`参考 / 标记 / 特效 / 角色库 / 运镜` 五个入口。
- Prompt:`描述你想要生成的画面内容，@引用素材`。
- 模型按钮:`2.0`(不再是 Seedance 2.5 全称;模型菜单未展开采样)。
- 模式:`文生视频`。
- 设置合并芯片:`16:9 · 720P · 5s · 1个 ·`(比例/清晰度/时长/数量合并为单一芯片,点击行为未采样)。
- 积分:`135`。
- 高级设置行:`高级设置 / 联网搜索 / 自动校验素材 / 智能引用 AutoLink`。
- 新功能条:`新功能：支持真人`。

## 与 clone 基线的差异(部分)

- clone 面板:Seedance 2.5 模型菜单、独立 比例/清晰度/时长/数量 控件、无 尝试行、无 工具行、AutoLink 在高级设置内、无 联网搜索/自动校验素材/新功能条。
- `SOURCE_UNKNOWN`:模型菜单展开内容、设置芯片点击面板、生成按钮文案、参考/标记/特效/运镜 各自交互、支持真人详情。

## 处置

Batch 125 实施默认态对齐(尝试行/工具行/模型 2.0/设置芯片/积分 135/高级设置行/新功能条);
未采样交互保持 clone 现状并记录。


## 尝试芯片联动采样补充（2026-09-06 晚间）

`SOURCE_FACT`（点击尝试芯片后设置芯片联动）:

- `5分钟超长视频` → 设置变为 `Auto · 720P · 300s · 1个`（比例 Auto、时长 300s）;
- `首尾帧生成视频` → `Auto · 720P · 5s · 1个`;
- `首帧生成视频` → 选中后面板状态变化,完整联动未完成采样。
- 取消选中的联动未采样。


## 参数菜单展开态探针（Batch 129,2026-09-06）

- 尝试选中 `5分钟超长视频` 后点击设置芯片的展开采样因会话退化未完成;
  设置芯片标签 `Auto · 720P · 300s · 1个` 已由 Batch 128 采得并落地。
- 参数菜单在尝试模式下的时长滑条范围/比例选项保持 `SOURCE_UNKNOWN`,
  待浏览器会话恢复后补采样。


## 积分数据点采样（Batch 130,2026-09-06）

`SOURCE_FACT`:

- `16:9 · 720P · 5s · 1个` → 积分 **135**;
- `Auto · 720P · 5s · 1个` → 积分 **230**;
- 即**比例影响积分**（Auto 比 16:9 贵 ~70%），clone 公式（时长×46×数量，与比例无关）
  存在公式漂移。

`BLOCKED`:完整公式推导需改变时长/数量后逐点读数,但参数菜单在该会话状态下
无法展开（自动化交互退化）。clone 公式保持现状并记录漂移;修复需会话稳定后
补采样（至少 4 个数据点:16:9/Auto × 5s/10s）。


## 采样阻塞记录（Batch 132,2026-09-06 深夜）

源站会话经多轮重载后严重退化：添加面板条目不再渲染、节点选择失效、
模型菜单无法展开。模型菜单内容、设置芯片展开态、积分公式推导均
`BLOCKED_BY_SESSION`，待浏览器会话刷新（重新登录）后补采。
已落地批次（125/126/128）不受影响——其断言基于当日已采得事实。


## 模型菜单全量采样（Batch 141,2026-09-07）

`SOURCE_FACT`（1100 高视口下展开模型菜单,滚动全量收集,**35 个视频模型**）:

Seedance 2.5(2min,最强视频模型，全能参考，30s音画同步)、Seedance 2.0 VIP(2min,会员专属通道,15s音画同步)、Minimax H3 Max(30s,后训练极速视频生成，支持文生、图生及首尾帧控制)、Minimax H3(2min,全模态输入，多参数控制，多场景商用级生成)、Seedance 2.0 Fast VIP(2min,快速版,15s音画同步)、Seedance 2.0 Mini(2min,mini版,高性价比,15s音画同步)、Wan 3.0 Prime(1min,超快速生成，全模态参考，超写实高一致性)、Wan 3.0(3min,全模态参考，支持文档与网页输入，超写实高一致性生成)、Happy Horse 1.1(3min,阿里最新视频模型，一致性与视听质量更可控)、Happy Horse 1.0(3min,阿里视频模型，支持多参生成)、Kling O3(3min,视频编辑模型、参考一致性、音画同出、多镜头)、Kling 3.0 Turbo(3min,高质感、支持多镜头)、Kling 3.0(3min,高质感、支持多镜头)、Wan 2.7(3min,全能参考，支持修改视频画面、剧情、环境)、Kling O1(3min,可灵一代编辑模型、支持多模态输入)、Wan 2.6(3min,音画同步，支持多机位镜头，最长可生15秒视频)、Hailuo 2.3(2min,善于表达动作、表情、镜头，更高质感)、Seedance1.5 Pro(2min,音画同步，最长可生12秒视频)、Seedance 1.0 Pro(2min,高精度提示词理解，40秒生成1080P视频)、Seedance 1.0 Lite(1min,轻量快速，一键进行日常视频生成)、Kling 2.6(2min,直出音画同步)、Hailuo 02(2min,画质稳定，适合打造运动特效场景)、Vidu Q2(3min,多图主体参考，精确控制效果佳)、Vidu Q2 Pro、Vidu Q2 Turbo、Vidu Q3 Pro(2min,支持主体参考，精确控制效果佳)、OmniHuman 1.5(3min,多模态数字人视频生成)、Kling 2.5(2min,速度快、效果稳定、性价比高)、Wan 2.2(3min,支持特效、玩法千变万化)、Wan 2.5(3min,支持特效、直出音画同步)、Pixverse V5.5(3min,支持特效、玩法丰富)、Pixverse V5(3min,支持特效、玩法丰富)、Hailuo 2.3 Fast(1min,更快速)、Kling3.0 动作迁移(8min,动作控制模型，需输入1张图片、1条视频)、Style Video(2min,图生视频效果稳定，画面表现力强)。

- 默认选中显示 `2.0`（对应 Seedance 2.0 系列简写）。
- 菜单为内部滚动列表（项目高度 ~56px/项）。
- `SOURCE_UNKNOWN`:各模型💎标记完整分布、分组标题（若有）。


## 默认选中态探针（Batch 142,2026-09-07,受阻）

- 模型菜单展开后各选项的选中态采样多次尝试均被会话退化阻断：
  添加面板开启后条目列表为空（间歇性,重载可暂缓但随即复发）。
- `SOURCE_UNKNOWN` 维持：源站默认选中模型（触发器显示 `2.0`）、
  尝试模式与参数菜单的联动展开态。
- 恢复条件：外部浏览器窗口刷新/重新登录后重试。
