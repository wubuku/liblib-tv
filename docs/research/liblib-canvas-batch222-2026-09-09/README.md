# Batch 222 — 参考选择模式交互采样（部分完成）

## 采样观察（源站 2026-09-09；`source-ref-select-click.json` / 截图存档）

- 参考 pill 选择模式横幅确认（蓝底、364×56、返回节点按钮 + ×）。
- 图片节点点击在选择模式下未直接填入参考 slot——模式持续存在，点击
  可能只是普通节点选择。参考填充的真实交互（拖拽连线 vs 点击选中）
  未定。
- 画布同时可见：空 image 节点 + video 节点 + 连线 + panel（底部）+
  Agent 抽屉（右侧 Skill 推荐区）。

## 处置

- 证据批：参考选择模式的完整交互语义（点选 → 填入 slot → 横幅消失）
  需要有实际图片内容的节点才能触发，当前图片节点为空态。
- 零 clone 代码改动。
- 源站测试残留清理：采样节点已删（0 残留）。

## 不证明

- 参考选择模式的点选填入行为（需有内容的图片节点）
- 拖拽连线 vs 点击选中的优先级
- 多参考的 max 数量限制
