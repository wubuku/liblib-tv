# Batch 286 — 画布切换下拉采样（源站 2026-09-10 直采）

> 状态：`SAMPLING_RECORDED`（下拉结构确证；子菜单未展开）。
>
> 证据：`canvas-dropdown.png`、`dropdown-dump.json`。

## 源站事实（`SOURCE_FACT`）

顶栏「画布 1 ∨」点击后的下拉（170×70 左上弹层）：

- Header：**「画布」** + **[+] 新建画布按钮**；
- 画布列表：**画布 1**（当前），行尾 **∨ 子菜单箭头**（重命名/删除等
  每画布操作——未展开采样）。

## 附带发现

- **我的工具箱浮层跨操作持久**：batch 284 打开后历经多次脚本会话
  （含页面状态清理）仍保持开启——面板关闭需显式点 ×（位置在面板
  右上）；
- clone 对齐状态：画布切换下拉 clone 已有（onOpenCanvasDropdown
  合同）——结构与源站一致性待对照（后续批次）。

## 验收

- docs check 通过；无画布节点操作。
