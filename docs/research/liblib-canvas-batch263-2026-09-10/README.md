# Batch 263 — 资产管理抽屉重采漂移 + 图片节点生成面板发现（源站 2026-09-10 直采）

> 状态：`SAMPLING_RECORDED`（发现量大于单批实施量，实施排后续批次）。
>
> 证据：`drawer.png`（抽屉 + 图片面板同框）、`sample-log.json`。

## 源站事实一：资产管理抽屉漂移（对照 batch 12 基线 240px）

- 宽度 **240 → 320px**；
- 页签 画布 / 资产 保留；
- 工具行新增：搜索 / 筛选 / **所有评级 ∨**（batch 146 评级体系）/
  列表视图切换（右端）；
- 节点条目：类型图标（图片/播放）+ 名称（图片节点 36 等）+ 悬停操作
  （… 菜单 / 发送箭头）；
- 抽屉底部：**← 折叠箭头 + 「共 3 节点」计数**。

## 源站事实二（重大新表面）：图片节点生成面板

选中图片节点后出现与视频面板同构的生成面板（`drawer.png` 右下）：

- 尝试建议：**图生图 / 图片高清**；
- 工具行：+参考 / 标记 / **风格**（无 特效/角色库/运镜）；
- 提示词占位：「可直接文生图，或上传图片输入文字指令对图片进行编辑，
  如：将背景改为雪夜」；
- 页脚：**Lib Image ∨ / 16:9 · 标准画质 · 2K · 1张 ∨ / 18 积分**——
  图片生成有自己的模型（Lib Image）、画质（标准画质）与分辨率（2K）
  维度。

## 实施排程建议

1. 抽屉漂移（320px + 评级过滤 + 底部计数 + 条目悬停操作）——中等规模；
2. 图片节点生成面板（尝试建议/pills/Lib Image 模型/画质分辨率维度）
   ——大规模，需独立采样批（参数菜单展开态/尝试联动/积分公式）。

## 验收

- docs check 通过；源站画布 **0 残留**；无代码变更。
