# Batch 317 — 展示设置/生成历史菜单深采不确定 + 需求槽节点内嵌形态证据归档（源站 2026-09-11）

> 状态：`INCONCLUSIVE`（两菜单均未重现）+ 证据归档（batch 311）。
>
> 证据：`B_history.png`（待确认生成节点需求槽全貌，已归档至
> batch 311 为 `pending-node-with-slots.png`）、`menus-log.json`。

## 记录

- **展示设置菜单未重现**（batch 300/308 同模式第三次）——aria-label
  驱动点击后菜单未展开；间歇性根因仍为浮层挂载时序；
- **生成历史 工具栏入口点击后覆盖层未出现**——与 batch 280 首采时
  的行为不一致，同样属间歇性；
- **有价值证据捕获**：待确认生成 视频节点的需求槽以**音频/图片缩略
  图形式直接渲染在节点底部**（音频 00:04 波形缩略 + 图片 1080×1446
  缩略）——OmniHuman 需求槽的节点内嵌形态首次完整可视，已归档至
  batch 311。

## 验收

- docs check 通过；源站画布 **0 残留**；无代码变更。
