# Batch 34 Code Archaeology

> 状态：待引入并固定上游子模块后填写。

本文件将记录固定 submodule commit 中的文件级事实：

- 构建入口、运行入口和目录结构；
- React / Three.js / React Three Fiber / Zustand 的依赖关系；
- 3D 场景、对象树、相机、灯光、角色和镜头的组件；
- 数据类型、store、持久化和 host bridge；
- 测试、脚本、资源目录和已知缺口。

所有引用都应带路径和必要的符号名。README 宣称但源码未找到的能力，
单独标记为 `README-only`，不能写成已验证实现。

