# Batch 303 — 双类型节点筛选联动重试：文本节点创建失败（源站 2026-09-11）

> 状态：`INCONCLUSIVE`（文本节点经添加面板创建失败——文本 条目点击
> 未产生节点；无代码变更）。
>
> 证据：`linkage-log.json`、`filter-image.png`、`filter-video.png`。

## 记录

- Escape-后-dblclick 配方本身工作（首节点 视频节点 15 建成）；
- 但第二节点（文本）经添加面板创建失败（created: False）——文本 条目
  点击未产生节点。与 batch 302 的失败模式一致：**添加面板第二次
  打开时条目点击不可靠**（面板重开状态问题）；
- 筛选联动再次无法分辨（仅 视频节点 15 单条，图片/视频 筛选均
  全列表）。

## 下一迭代要点

- 探针需先诊断添加面板第二次打开的状态（重开面板后条目列表是否
  渲染、点击目标是否存在），或改用 逐帧拉片 等其它条目类型重试；
- 或以批次 33 合同为准：clone 的筛选联动（batch 205 已验）保持，
  源站联动深采转入人工。

## 验收

- docs check 通过；源站画布 **0 残留**；无代码变更。
