# Batch 257 — OmniHuman 音频需求机制发现（源站 2026-09-10 直采）

> 状态：`SAMPLING_RECORDED`（音频连线采样留待后续——需边拖拽自动化）。
>
> 证据：`after-audio-click.png`（需求行红色缺失态 + 面板全貌）。

## 源站事实（`SOURCE_FACT`）

- **缺失需求红色标记**：`音频 0/1` 以**红色**渲染（图片 1/1 为正常白/灰
  ——已满足态无高亮）；「请提供音频」灰显；
- **音频标签点击为无操作**：无文件选择器、无面板变化（4s 监听）——
  音频需求**不经上传满足**；
- **需求满足机制推断（强证据）**：`图片 1/1` 的满足状态来自已连入的
  图片节点边（首帧芯片自动创建）→ 音频需求同理需**连入音频节点**；
- 面板保留首帧说明文案「以当前图为首帧生成视频。」（batch 248 截图
  复认）。

## 采样缺口

- 音频节点连入后的满足态（音频 1/1、警示消失、积分变化）——需边拖拽
  自动化（源→目标把手 mouse down/move/up），留待后续配方；
- 请提供音频 警示在满足后是否消失/变绿。

## 方法学备注

- 源站节点 id 非 `video-` 前缀（重选探针曾误用 clone 命名约定）；
- `window.__libtv_store` 为 clone 专属，源站探针只能用文本/几何。

## 验收

- docs check 通过；源站画布 **0 残留**；无代码变更。
