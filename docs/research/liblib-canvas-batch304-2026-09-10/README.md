# Batch 304 — 添加面板二次失败根因确诊 + 筛选联动部分验证（源站 2026-09-10）

> 状态：`ROOT_CAUSE_FOUND`（二次失败根因确诊 + 混合类型筛选行为
> 部分验证；无代码变更）。
>
> 证据：`panel-state.png`（面板未重开瞬态）、`filter-video-mixed.png`。

## 根因确诊（batch 303 遗留）

**第二次 dblclick 落在第一个节点（759×1006px）覆盖区内**——React Flow
节点双击不打开添加面板，面板根本未重开（诊断探针 panelFound: false
实证）。此前各批的「第二次条目点击不可靠」均为**同一根因的表象**
（面板未开 → 点击落空），非面板渲染缺陷。

**配方修正**：第二节点创建的 dblclick 必须避开首节点覆盖区——本批
+900px 右偏移后两次创建连续成功（视频+文本，2 节点）。

## 筛选联动部分验证（混合类型，filter-video-mixed.png）

视频 + 文本 两节点并存，抽屉筛选选 **视频** 类型：

- 列表 = **文本节点 18 + 视频节点 17 双条目全显**——筛选未过滤！

**漂移发现**：源站抽屉的「筛选」未按类型过滤条目（与 batch 205
时代 clone 合同不符）。结合 batch 297（展示设置 = 视图布局菜单）、
batch 301（筛选菜单 = 十项类型清单）——**源站筛选菜单当前为纯展示
选择器或未接线**，clone 的类型过滤联动为超出源站的行为。

## clone 处置建议

clone 筛选联动（batch 205 合同）为**超出源站的附加行为**——保留
（原型增强）或在后续对齐批移除，记录待定。

## 验收

- docs check 通过；源站画布 **0 残留**；无代码变更。
