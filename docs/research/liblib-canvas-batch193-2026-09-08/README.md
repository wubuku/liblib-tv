# Batch 193 — 参考/标记前置条件采样（提示词态仍惰性，固化为合同）

## 源站事实（窗口 rAF ~31fps；`source-pill-precondition.json` / 截图已存档）

- 前置态 A（节点已输入提示词「一只猫在窗台看雨」）：参考/标记点击
  **仍惰性**（无弹层）。
- 前置态 B（连接素材节点，经特效卡流程尝试）：采样未完成——特效库
  JS 点击未开（页面状态漂移），素材前置未真正建立。
- 结论：参考/标记的前置条件在「空节点」「有提示词」两态下均不触发；
  真实前置（推测需上传/生成的参考图内容）仍未定位。

## 实施

- 零代码改动（clone 的参考/标记本就是惰性占位，与全部已采样状态一致）。
- 新增 `verify-liblib-batch193.py`（6 checks）：输入提示词后点击
  参考/标记 → 无运镜菜单/特效库/右键菜单打开、面板与提示词保持——把
  「提示词态惰性」固化为回归合同。

## 验收

- `verify-liblib-batch193.py`：6 checks 一次通过。
- 回归绿：146 / 151 / 172 / 191；`npm run check` 0 errors；docs check 通过。
- 不证明：素材连接前置（B 态采样未完成）；真实参考图上传后的行为。
- 源站测试残留清理：采样节点已删（0 残留）。
