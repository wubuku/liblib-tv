# Batch 342 — 顶栏 chrome 对照审计（`INVESTIGATION_RECORDED`，实施暂缓）

> 状态：对照调查完成并留档；**实施暂缓**（用户指令留档收批）。
> 证据：clone 顶栏 aria/text 转储（1920×900）× 源站 2026-09-11
> 截图与按钮文本转储（batch 332-340 会话）。

## 对照发现（待实施，全部有源站直证）

clone 顶栏（左→右实测）：logo(A) / 项目菜单(∨) / 工作区名称输入 /
画布 chip / 工作流 / 故事板 ‖ 发布与分享 / **积分超市(🏆)** /
积分余额(⚡100) / 开通会员限时45折 / Agent。

源站 2026-09-11 直证（截图 probe332_state/workbench + 按钮 text
转储，会话内三次独立采样一致）：

1. **源站顶栏无「积分超市」按钮**——batch 139（2026-09-05/07 时代）
   的双入口之一已不在现版顶栏（全部转储与截图均无 积分超市 文本）；
   现版仅剩 ⚡100 积分余额入口；
2. **顺序**：源站为 分享图标 → 蓝色图标(SOURCE_UNKNOWN，未辨识) →
   开通会员(限时45折) → ⚡100 → 头像(圆形) → Agent；
   clone 为 分享 → 积分超市 → 余额 → 会员 → Agent——
   **开通会员应在 积分余额 之前**；
3. 顶栏蓝色小图标（源站 ~1495px 处）身份未辨识（SOURCE_UNKNOWN），
   源站顶栏另有圆形头像入口（clone 未见对应物）。

## 实施清单（下批执行）

- [ ] 移除 积分超市 按钮（TopNavBar，batch 139 注释同步更新）；
- [ ] 开通会员 与 积分余额 交换顺序（会员在前）；
- [ ] 蓝色图标/头像标记 SOURCE_UNKNOWN 待采样；
- [ ] 迁移受影响验证器：121（入口清单含 积分超市）、139（双入口+
  顺序断言）、168（引用 积分超市）；
- [ ] 新验证器固化 2026-09-11 顶栏直证（标签 + 顺序）。

## 当前工作区状态

- 工作树干净（aa27f7f = batch 341）；本批无源码变更；
- 源站画布渲染劣化持续，BLOCKED_SOURCE 三项维持阻塞。

## 后续候选

- 上表实施清单（下批首要）；
- 源站恢复后 BLOCKED_SOURCE 补采与 CLONE_DECISION 替换；
- 头像入口与蓝色图标身份采样。
