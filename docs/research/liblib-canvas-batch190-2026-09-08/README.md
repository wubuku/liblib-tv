# Batch 190 — 比例格模型依赖假设复测（假设否定，网格统一 7 格）

## 复测设计（源站 2026-09-08，窗口 rAF ~31fps；`source-ratio-tiles-by-model.json` / 截图存档）

- 新建节点（今日默认模型 2.5）：普通模式参数菜单 7 格含 Auto（与
  Batch 189 一致）。
- CDP 度量覆盖抬高视口后模型菜单可开（此前 JS click 打不开的根因：
  触发器贴近视口下缘，菜单翻转/裁剪）→ 切到 **Seedance 2.0 VIP** →
  重开参数菜单。

## 结果：假设否定

- 模型 2.0 下比例格 **仍为 7 格含 Auto**（与 2.5 完全一致）。
- 「比例格随模型变化」假设不成立；Batch 176 的「长 7 / 普 6」分割与
  Batch 175 的 6 格采样均被今日双向直采推翻（昨日 6 格的采样条件已无法
  复验，按当前证据迁移）。
- 附带：今日默认模型 2.5（昨日 2.0）——账号态默认继续随机波动，维持
  clone 2.5 不翻转。

## 实施

- `ParamsMenu` 比例格统一为 **7 格含 Auto**（`grid-cols-5`，两模式同）；
  Auto 仍仅在长模式选中（尝试芯片联动不变）。
- 验证器迁移：batch175（6 格→7 格含 Auto、顺序含 Auto）、batch21
  （assert_common_controls 6/7 分支→恒 7）。
- 采样方法沉淀：模型菜单在触发器贴近视口下缘时 JS click 打不开，需
  CDP `Emulation.setDeviceMetricsOverride` 抬高视口（batch 174 配方）。

## 验收

- batch175 / 21 / 176 / 189 迁移后全绿；全量回归 24 项绿（22-188）。
- `npm run check`：0 errors（8 warnings 基线）；docs check 通过。
- 不证明：1.x/其它模型族的格数（仅 2.5/2.0 双向直采）；昨日 6 格采样的
  真实成因。
- 源站测试残留清理：采样节点已删（0 残留）。
