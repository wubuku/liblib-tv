# Batch 112 Plan：角色筛选面板对齐 2026-09-05 补采样

> 状态：`IN_PROGRESS`
>
> 建档日期：2026-09-05。
>
> 上一批 checkpoint：Batch 111（`batch/111-character-modal-source`）。
>
> 源站证据：2026-09-05 补采样（`original-character-filter-panel.png`，只读打开）。

## 1. 采样事实（`SOURCE_FACT`）

`角色筛选` 点击展开向上面板：头部 `角色筛选` + `清空筛选`（带刷新图标）；
分组芯片：

- 性别：男 / 女 / 中性
- 年龄段：儿童 / 少年 / 青年 / 中年 / 老年
- 种族：人类 / 精灵 / 兽人 / 机械 / 其他
- 时代：先秦 / 古代 / 近代 / 现代 / 未来
- 文化区域：选项被卡片条遮挡（`SOURCE_UNKNOWN`，仅组名可见）

## 2. 范围

### 包含

1. clone 角色库的 `角色筛选` 按钮展开同构筛选面板（组名/芯片集合按采样），
   `清空筛选` 一键清空。
2. 过滤语义（`CLONE_DECISION`）：组内多选、组间 AND；芯片与角色标签
   （tagsFor 输出）相等匹配，`古代` 别名匹配 `古风`；无对应标签数据的组
   （种族/文化区域/先秦/近代/未来）选中后无角色可满足（空结果可见，诚实）。
3. `最近使用` checkbox 维持现状（源站同为 checkbox，行为未采样）。

### 不包含

- 真实筛选后端、`文化区域` 未知选项、源站芯片选中态视觉。

## 3. 证据边界

`SOURCE_FACT`：面板存在、组名、芯片集合、清空筛选、向上展开；
`CLONE_DECISION`：过滤语义与别名映射；`SOURCE_UNKNOWN`：文化区域选项、
芯片选中样式、与详情标签的联动。

## 4. 验证

- `scripts/verify-liblib-batch112.py`：1440x900，断言面板五组芯片集合、
  选 `男` 后甜妹卡隐藏且霸总卡保留、`清空筛选` 恢复全量、关闭面板；
  零诊断。
- 复跑 batch111/11；`npm run check`、`npm run docs:check`。

## 5. 完成定义

筛选面板与采样结构一致、本地过滤语义生效；相邻回归与全量门通过；
特性分支 commit/push。
