# Batch 289 — 素材库工具栏条目实为库快捷弹层（源站 2026-09-10 直采）

> 状态：`SAMPLING_RECORDED`（无代码变更）。
>
> 证据：`sucaiku-body.png`（弹层全貌）。

## 源站事实（`SOURCE_FACT`）

底部工具栏「素材库」点击打开的是**小型弹层**（~150px 宽，非侧栏）：

- 标题：素材库；
- 内容：**风格库 NEW** 一条（带图标；batch 280 采样时另有 特效库 NEW
  ——条目可能按上下文/加载态变化，`SOURCE_UNKNOWN`）；
- **无上传供体**——素材上传面不在本弹层，仍在素材库门后
  （需用户授权承诺书，维持阻塞）。

## 判定

「素材库」工具栏条目实为**库快捷入口弹层**（导航至风格库/特效库），
非素材管理面板。上传交互的采样缺口与素材库门后阻塞合并，维持。

## 方法学备注

小型弹层（<300px）会被宽面板检测器漏检——探针宽度过滤需放宽。

## 验收

- docs check 通过；无画布节点操作；无代码变更。
