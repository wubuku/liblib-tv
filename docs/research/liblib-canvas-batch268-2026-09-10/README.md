# Batch 268 — 图片高清一键预设生成 clone 实施（源站 2026-09-10 采样落地）

> 状态：`IMPLEMENTED`（batch 267 采样 → 实施 → 11 checks 验收 → 37 项
> 回归绿）。
>
> 源站证据：`../liblib-canvas-batch267-2026-09-10/disambig-late.png`。

## 复刻内容（batch 267 采样事实）

1. **空图片节点尝试建议行**：`尝试：` + 图生图 / 图片高清 芯片
   （batch 263 截图；本地视觉态 CLONE_DECISION——图片 attempt 的源站
   持久化未采样）；
2. **图片高清 = 一键预设生成**：`createImageHdPreset` 创建
   「预设 - 图片高清」storyboard-group 容器（430×452，zIndex -1001 同
   分镜组先例）+ 内含 `aiGenerated: true` 图片子节点（parentId 嵌套）；
   **选中迁移至容器**（源图片节点脱选，setTimeout(0) 赢过 React Flow
   点击选中路由的写回竞态）；单条历史记录；
3. **图片节点空占位创建**：添加面板 图片 条目创建的节点不再带默认
   咖啡示例图（`imageUrl: null`）——源站新建图片节点为山形占位
   （batch 263 截图），且尝试建议行依赖空态渲染；
4. **AI生成 徽章**：`data.aiGenerated` 时图片媒体容器左上角渲染。

## 验收

- `verify-liblib-batch268.py`：**11 checks**（尝试行/两芯片；图生图
  视觉提交；图片高清 → 容器+子节点 aiGenerated+选中迁移+2 节点；
  容器重选；抽屉入口回归）。
- 回归绿：21 / 22 / 26 / 33 / 100 / 111 / 125 / 128 / 146 / 149 / 151 /
  155 / 160 / 165 / 166 / 172 / 173 / 174 / 175 / 176 / 177 / 178 / 189 /
  191 / 213 / 215 / 218 / 236 / 237 / 238 / 239 / 240 / 244 / 248 / 249 /
  252 / 255（37 项）。
- `npm run check`：0 errors（8 warnings 基线）；docs check 通过。

## 不证明 / 后续候选

- 图生图芯片的源站点击语义（面板行为未采样到差异，视觉提交）；
- 输出边的连接目标（batch 267 记录：源站容器右侧延伸，目标未采样）；
- 预设容器的展开/折叠交互。
