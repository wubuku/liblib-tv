# Batch 176 — 长视频模式参数菜单 + 长芯片模型联动（源站 2026-09-08 直采）

## 源站事实（窗口 rAF ~31fps，`source-long-params-menu.json` / 截图已存档）

- 节点卡「5分钟超长视频」芯片点击后：页脚 **模型 2.0 → 2.5**（长视频
  管线模型，与 Batch 33 过程节点 model=2.5 合同互证）、模式 超长视频、
  参数摘要 `Auto · 720P · 300s · 1个 ·`。
- 长模式参数菜单 340×396：
  - 比例网格 **7 格含 Auto**（Auto 首位、选中 bg white/10；每行 5 格、
    57×62 瓦片）——修正 Batch 175「Auto 仅是内部状态」的范围：Auto 瓦片
    在长模式真实存在，普通模式确实没有；
  - 清晰度 480P/720P/1080P（100×32，720P 选中）；
  - 时长滑杆 input min=30 max=300 value=300 + 长模式专属提示
    **「因剧情和画面设计，实际时长可能略有差异」**；
  - 生成音频 开启（选中）/关闭；**数量区在长模式下不渲染**。

## 实施

- `ParamsMenu`：比例列表按模式切换——长 7 格（Auto 首位、grid-cols-5）/
  普通 6 格（grid-cols-4）；长提示文案替换为源站原文（原
  「围绕视频画面设计…」为早期推断文案）。
- 尝试联动：5分钟超长视频芯片增加 `setModel("2.5")`（源站页脚直证）；
  取消芯片不回切模型（取消行为源站未采样，维持单向）。

## 验收

- `verify-liblib-batch176.py`：12 checks（芯片→模型 2.5/模式/摘要/
  积分 14700；长菜单 7 格含 Auto 选中/62 瓦片/30..300/提示原文/无数量区）。
- `verify-liblib-batch21.py` 迁移：长阶段比例断言 7 格含 Auto 选中
  （普通阶段 6 格合同不变）。
- 回归绿：22 / 26 / 33 / 128 / 149 / 155 / 160 / 165 / 166 / 172 / 173 /
  174 / 175。
- `npm run check`：0 errors（8 warnings 基线）；docs check 通过。
- 不证明：取消芯片后模型是否回切；长模式下分辨率/音频的可点规则
  （采样时全部可点状态未逐一验证）；非 2.0 起点模型下长芯片的模型切换。
- 源站测试残留清理：采样用视频节点已删（0 残留）。
