# Batch 191 — 工具条 pill 弹出面采样 + 特效库横排实装

## 源站事实（窗口 rAF ~31fps；`source-pill-popovers.json` / `source-effects-card.json` / 截图已存档）

- **运镜 / 参考 / 标记**：空节点上点击**惰性**（无弹层——与 footer 按钮
  同款模式，前置条件未采样）。
- **特效**：打开效果库横排——屏幕居中悬浮，8 卡横排（clone 渲染已采样
  的 4 张）；卡片 185×235：图区 178×178（远端 webp 缩略图，如
  `.../tool/cinematic/transition_5.webp`）+ hover 层（拖拽三点钮、
  收藏钮）+ 名称 + 商用标 + 作者 + 积分。
  已采样 4 张：试妆特写/捏捏AI/185、悬浮缓入/捏捏AI/377、
  微距推镜/可可大王/659、直升机揭幕/汪往旺/144。
- 展开时覆盖触发器与面板提示词区（z 高于面板）——关闭靠外部点击。

## 实施

- 特效 pill（`data-effects-trigger`）toggle 特效库横排
  （`data-effects-gallery`，fixed bottom-[175px] 屏幕居中、横向滚动）；
  4 张已采样卡片：渐变占位缩略图（源图仅采到一张 URL，全部用占位保持
  一致）、hover 收藏钮（原爱心 path）、名称/商用标/作者/积分；
  外部 mousedown 关闭（展开覆盖触发器，源站同构）。
- 运镜菜单（batch146 现行合同）保留；源站空节点惰性记为分歧/开放问题。

## 验收

- `verify-liblib-batch191.py`：10 checks（初关/打开/四卡/名称顺序/
  卡宽 185/商用标/积分/外部关闭/运镜合同不回归/0 console error）。
- 回归绿：146（运镜 19 checks）/ 151 / 166 / 172 / 186 / 188。
- `npm run check`：0 errors（8 warnings 基线）；docs check 通过。
- 不证明：特效卡点击后的选用行为（含积分扣除）；运镜/参考/标记的
  前置条件；8 卡中其余 4 张的内容；真实缩略图（渐变占位）。
- 源站测试残留清理：采样节点已删（0 残留）。
