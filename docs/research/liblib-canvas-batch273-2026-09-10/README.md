# Batch 273 — Agent 抽屉重采：第五种 headline 漂移对齐（源站 2026-09-10 直采）

> 状态：`IMPLEMENTED`（重采 → 漂移对齐）。
>
> 证据：`agent-drawer.png`、`drawer-dump.json`。

## 重采结果：结构无漂移

源站 Agent 抽屉（400px，右侧停靠）与 clone 逐项对齐：headline 行 +
换一批、双列技能卡（皮克斯动画广告 /pixar-animated-ad-creator、爆款
拉片复刻、新中式美学TVC、古典武侠电影全流程导演——clone batch 201
目录全含）、浏览器通知条（开启浏览器通知…/开启）。

## 漂移发现与对齐

**第五种 headline「每个 Skill，都是一个开场」**（batch 107 三种 +
batch 199 第四种之后的第五种）——已加入 clone 的 `skillHeadlines`
轮换数组。

## 验收

- typecheck 通过；docs check 通过（833 文件）；无源站画布操作。
