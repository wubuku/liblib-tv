# Batch 236 — 模型切换长视频态重置 + 4K 随模型 + 积分模型对照（源站 2026-09-09 直采）

> 状态：`IMPLEMENTED`（采样 → 实施 → 23 checks 验收 → 全量回归绿）。
>
> 环境：外部 Chrome for Testing 147（CDP 9222），登录态；CDP
> `Emulation.setDeviceMetricsOverride` 1920×2400 抬高视口（batch 174/190 配方）。
> 证据：本目录 `p1..p6` 截图与 JSON、`phase2-log.json`、`s*/r*` 过程记录。

## 采样过程要点

- 双击空画布 → 添加面板 → 视频条目创建节点（759×1006 大卡）。
- **新建空视频节点选中后无面板**，节点卡内出现「尝试：」三芯片建议
  （batch 153 记录的「无尝试建议」与今日不符——今日直证芯片出现）。
- 点击「5分钟超长视频」芯片 → 生成器面板为**懒加载 chunk**，首次加载失败：
  页脚出现 **生成器加载失败 / Failed to load chunk https://libliba… / 点击重试**
  （新表面事实）；整页 reload 后重击芯片，面板正常挂载。
- 积分显示**无「积分」字样**，仅为 ⚡ 数字（此前按「积分」文案探测因此漏采）。

## 源站事实（`SOURCE_FACT`，双向对照）

### 模型切换重置长视频态（p2/p5 截图）

2.5 + 超长视频（Auto·720P·300s·1个，⚡14700）下打开模型菜单选 **Seedance 2.0 VIP**：

- 模式重置 **超长视频 → 文生视频**；时长 **300s → 15s**（与 2.0 VIP
  「15s音画同步」上限一致）；设置芯片变 `Auto · 720P · 15s · 1个 ·`；
- **尝试芯片高亮清除**（节点卡内三芯片均无选中态）；
- 模型触发器缩写 **2.5 → 2.0**（batch 149 缩写规则复认）；
- **切回 2.5 不恢复长模式**：模式保持 文生视频、15s。

### 清晰度列表随模型（p3 vs p6 同轮对照）

- 2.0 VIP：**480P / 720P / 1080P / 4K**（4 项一行）；
- 2.5：480P / 720P / 1080P（3 项）；其它模型族未采样（`SOURCE_UNKNOWN`）。

### 积分模型对照（同参数 A/B）

| 模型 | 模式 | 参数 | 积分 | 速率 |
|---|---|---|---|---|
| 2.5 | 超长视频 | Auto·720P·300s·1个 | 14700 | 49/s |
| 2.0 VIP | 文生视频 | Auto·720P·15s·1个 | **405** | 27/s |
| 2.5 | 文生视频 | Auto·720P·15s·1个 | **690** | 46/s |

### 比例格（复认 batch 190）

两模型下参数菜单比例格均为 **7 格含 Auto**（5 列两行），无模型差异；
本次 Auto 残留进入 文生视频 模式后菜单仍显示 Auto 瓦片并选中——进一步
支持 batch 190 的「网格统一 7 格、状态（非模式）决定 Auto 在列」结论。

### Batch 130「比例影响积分」结论修正（证据级推断）

Batch 130 的 Auto·5s=230 与 16:9·5s=135 A/B 对照早于 Batch 176 发现的
「长芯片静默切模型 2.5」；其 Auto 读数按 **2.5 态**解释后，全部 5 个数据点
自洽：**16:9 恒 27/s；Auto 溢价（46/s）仅存在于 2.5**，2.0 系 Auto 亦为
27/s（今日 405 直证）。

### 附带观察

- 节点命名计数器项目级持久：画布清空后新建节点仍从「视频节点 2」起编号
  （删除不重置计数）。
- 模型菜单行结构复认 batch 141/174：名称 + 生成时长徽标 + 描述 + 💎。

## 实施（`VideoGenerationPanel.tsx`）

1. `selectModel`：长视频态切换到非 2.5 模型时重置——清尝试芯片
   （`onAttemptChange(null)`）、模式回 文生视频、时长 15s、退出过程/提交态；
   切回 2.5 不恢复（源站直证）。
2. `ParamsMenu` 新增 `model` prop：清晰度列表 2.0 VIP 为 4 项含 4K
   （`grid-cols-4`），其余 3 项；切离 2.0 VIP 时 4K 钳制 1080P
   （`CLONE_DECISION`，源站未采样）。
3. 积分公式模型感知：`ratio === "16:9" || model !== "2.5" ? 27 : 46`
   （长视频 49/s 不变）——覆盖上表全部数据点，标注证据级推断。

## 验收

- `verify-liblib-batch236.py`：**23 checks**（长态 14700 → 切 2.0 VIP 重置
  断言 8 项；VIP 菜单 4 项含 4K/7 比例格；切回 2.5 保持文生视频/690；
  4K 钳制 1080P/690）。
- 回归绿：21 / 22 / 26 / 33 / 100 / 111 / 128 / 141 / 145 / 146 / 149 /
  151 / 155 / 160 / 165 / 166 / 172 / 173 / 174 / 175 / 176 / 177 / 178 /
  189 / 191 / 213 / 215 / 218。
- `npm run check`：0 errors（8 warnings 基线）。
- 源站测试残留清理：采样节点已删（**0 残留**）。

## 不证明（`SOURCE_UNKNOWN`）

- 其它模型族（Minimax/Wan/Kling/1.x 系）的清晰度列表与 Auto 速率；
- 2.0 系 ·16:9·5s（135 读数的模型态归属按 2.0 解释，未直接复测）；
- 模型切换对**非长视频**自定义参数（比例/清晰度/音频/数量）的影响
  （今日两侧均继承 Auto·720P·1个，未见重置）；
- 「生成器加载失败」重试链路的完整行为（点击重试未复现成功路径）。
