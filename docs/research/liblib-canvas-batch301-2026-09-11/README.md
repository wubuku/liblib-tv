# Batch 301 — 抽屉双菜单单会话采样确证（源站 2026-09-11 直采）

> 状态：`CONFIRMED`（双菜单全采；clone 现实现与源站完全匹配；无代码
> 变更）。
>
> 证据：`A_display_menu.png`、`B_filter_menu.png`、`menus-log.json`。

## 源站事实（`SOURCE_FACT`）

单会话内两菜单全采成功（batch 296/300 间歇性的根因：**必须单会话
连续操作**——本轮创建节点→抽屉→两菜单连续驱动，全部命中）：

### A. 展示设置菜单（180×167）

列表展示 / 宫格展示 / 展开全部分组 / 收起全部分组——与 batch 297
采样逐字一致；宫格展示 点击应用成功（batch 298 clone 实施的对照
验证 ✓）。

### B. 筛选菜单（180×374）——**十项与 clone 逐字一致**

全部 / 文本 / 图片 / 视频 / 智能剪辑 / 导演台 / 逐帧拉片 / 音频 /
脚本 / 脚本（旧版）。

## 三个记录闭合

1. **batch 296 的 SOURCE_UNKNOWN 解除**：筛选菜单 = 十项类型菜单，
   挂在 **筛选** 按钮上；
2. **batch 297 的「展示设置=视图布局菜单」判定确证**；
3. **batch 298 实施反向确证**：clone 现结构（筛选=类型菜单十项 +
   展示设置=视图布局四项）与源站**完全匹配**——batch 204 时代把
   类型菜单挂在 展示设置 上确为当时源站形态，其后源站迁移，
   batch 298 的迁移与源站演化一致。

## 验收

- docs check 通过；源站画布 **0 残留**；无代码变更。
