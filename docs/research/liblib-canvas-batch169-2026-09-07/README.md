# Batch 169 — 公共角色库模态页签 + 承诺书门模拟（源站 2026-09-07 补采）

## 窗口短暂前置的补采

源站窗口短暂可渲染（角色库模态成功挂载），补到两组结构：

1. **公共角色库模态 chrome**：1280×703、rounded-xl、header h-12 带 border-b，
   两个页签（公共角色库 / Seedance2.0&2.5合规素材库，gap-9、text-[14px]），
   遮罩 bg-black/20。
2. **Seedance2.0 承诺书门**：素材库内容被 738 宽承诺书对话框拦截
   （3 条承诺条款 + 同意勾选 + 不同意/同意并使用）。

## 采样伦理边界（留档）

承诺书为账号级法律同意。自动化过程中**未代用户勾选或点「同意并使用」**，
仅点「不同意」恢复原状（可逆，用户日后可自行同意）；素材库实际内容因此
仍未采样（留档阻塞）。

## 实施（clone 本地模拟）

- `CharacterLibraryPanel` header 改为源站页签 chrome（data-clib-tabs，
  公共角色库默认激活；素材库页签未同意承诺书时唤起承诺书门）。
- 承诺书门为**本地状态模拟**：同意并使用 → 本地标记 + 切到素材库空态页签；
  不同意 → 关闭对话框留在公共角色库。无任何账号/网络操作。
- 素材库页签内容为本地空态（「素材内容未接入」）。

## 验收

- `verify-liblib-batch169.py`：12 checks（页签顺序/激活态/承诺书条款/
  不同意回落/同意切换素材库空态）。
- 相邻回归绿：111 / 146 / 147。
- `npm run check`：0 errors、8 warnings（既有基线）。

## 仍阻塞

模型菜单选中态/300s 参数展开：模态可挂载后再次尝试点击模型触发器仍不挂载
菜单（窗口可见期与点击时机未重叠，遮挡限流间歇出现）。
