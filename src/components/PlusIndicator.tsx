"use client";

/**
 * PlusIndicator is no longer used.
 *
 * The original LibTV site uses the React Flow `<Handle>` component itself
 * (0×0 transparent) as the draggable connection point, with a custom
 * CSS appearance that shows a "+" circle on node hover.
 *
 * See globals.css `.react-flow__handle` rules.
 */

export function PlusIndicator() {
  return null;
}